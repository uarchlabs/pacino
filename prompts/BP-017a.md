=============================================================
# Task Header
=============================================================
:: HEADER:START ::
| Field        | Value                   | Notes                    |
|--------------|-------------------------|--------------------------|
| Task ID      | BP-017a                 |                          |
| Date         | 2026.04.09              |                          |
| Module       | bp_structs_pkg.sv       |                          |
| Run time     | 2m.22s                  |                          |
| Ctx %        | 21%                     |                          |
| Model        | Sonnet 4.6 medium       |                          |
| Resume sha   | efda41ef-e49d-4b96-812c-97b1bc18e805 |             |
| PA session  | 025 | |
Task:   [ ] experiment  [ ] implementation  [ ] debug       [x] cleanup [ ] testbench
Mode:   [x] automated   [ ] manual
Status: [ ] in-progress [x] complete        [ ] abandoned
# Task Overview
Cleanup pass: loop predictor interface doc and struct
naming. CLI-001, CLI-002, CLI-004.
CLI-001: add lp_hit to loop_pred_interfaces.md field
  lists (pred_p0 and upd_p0 sections).
CLI-002: audit and correct idx/tag field name references
  in loop_pred_interfaces.md against current RTL names.
CLI-004: rename lp_set -> lp_idx in bp_loop_meta_t.
No RTL files touched. No sim required.
:: HEADER:END ::
=============================================================
# Paste c.code console output and c.ai discussion
=============================================================
:: DISCUSSION:START ::
# Results Discussion
## Claude.code Console Output

  CLI-004 — bp_structs_pkg.sv                                                   
  - bp_loop_meta_t.lp_set renamed to lp_idx. No RTL consumer found. Only non-RTL
   references: planning/arch/bp_cluster.md (planning doc, not a struct          
  consumer), plus handoff/prompt files.      
                                                                                
  CLI-001 — loop_pred_interfaces.md                                             
  - lp_hit entry added to pred_p0 field semantics, after way, before age.       
  - lp_hit added to the flat field list in upd_p0 field semantics.              
  
  CLI-002 — loop_pred_interfaces.md                                             
  - Full audit of all field names in both pred_p0 and upd_p0 sections against
  lp_pred_t and lp_upd_t. Zero mismatches found. No corrections required.       
                                                                         
  Follow-on item flagged: planning/arch/bp_cluster.md:411 still references      
  lp_set. Outside scope of BP-017a per constraints — needs a follow-on doc pass.

## My Assessment

Pending

## Claude.ai Assessment

Assessment:

CLI-001: done correctly. lp_hit placed after way in field order matching lp_pred_t struct order — good judgment call on the ambiguous prompt wording.
CLI-002: full audit documented with every field checked. Zero mismatches confirms the doc was already consistent with RTL field names. This is the result we wanted.
CLI-004: search was thorough, correctly distinguished RTL consumers from planning/tracking files, proceeded with rename. bp_cluster.md reference flagged appropriately.

Ready for BP-017b. Scope is:

CLI-008: apply consistent lp_ prefix to all unprefixed fields in lp_pred_t and lp_upd_t
CLI-011: update loop_pred.sv and tb_loop_pred.sv to match renamed fields
Sim run: all 12 existing tests must pass

## Follow-on Actions
- [x] Update PROJECT_STATUS.md: close CLI-001, CLI-002,
      CLI-004 debt items.
- [x] Confirm bp_loop_meta_t lp_set->lp_idx rename does
      not break any existing consumer. No RTL consumer
      exists yet -- verify by grep before accepting.
## CLAUDE.md Updates
None
## Other Planning File Updates
_set to _idx fixed planning/arch/bp_cluster.md
:: DISCUSSION:END ::
=============================================================
# Claude.code Prompt
=============================================================
:: PROMPT:START ::
## Task ID
BP-017a

## Context Loaded
@frontend/branch_predictor/rtl/bp_structs_pkg.sv
@planning/interfaces/loop_pred_interfaces.md
@prompts/BP-017a.md

## Hypothesis
Three naming issues exist in the loop predictor interface
doc and structs. None affect currently compiling RTL.
All three are doc/struct-only fixes needed before
bp_cluster integration.

## Background

CLI-001: lp_hit is present in lp_pred_t and lp_upd_t in
bp_structs_pkg.sv but is absent from the pred_p0 field
semantics section and upd_p0 field semantics section of
loop_pred_interfaces.md. The field needs a description
entry added to both sections.

CLI-002: loop_pred_interfaces.md was written before the
current struct field names were settled. The interface
doc must be audited against the current lp_pred_t and
lp_upd_t field names in bp_structs_pkg.sv. Any field
name in the doc that does not match the struct must be
corrected to match the struct. The struct is the
authority -- do not change the struct to match the doc.

CLI-004: bp_loop_meta_t in bp_structs_pkg.sv has a field
named lp_set. The correct name is lp_idx, consistent
with lp_pred_t (which uses idx) and the interface doc
(which uses idx throughout). This is a struct-only
rename. bp_loop_meta_t has no RTL consumer yet -- verify
by searching the repo before making the change and
document the search result in Results Capture.

## Binding Previous Decisions
- bp_structs_pkg.sv is the single source of truth for
  all BP cluster struct definitions.
- loop_pred_interfaces.md describes port semantics and
  timing. It does not define struct layouts -- it
  references them. When doc and struct disagree, the
  struct wins.
- lp_pred_t field names are authoritative for the
  prediction output interface. lp_upd_t field names
  are authoritative for the update input interface.
- bp_loop_meta_t is the FTQ slow-path metadata struct
  for the loop predictor. It mirrors lp_pred_t fields
  with lp_ prefix. lp_set is the only field that
  diverges from lp_pred_t naming (idx vs lp_set/lp_idx).

## Specific Requirements

### CLI-001
1. In loop_pred_interfaces.md, locate the pred_p0 field
   semantics section. Add an entry for lp_hit after the
   way entry and before pred_is_loop. Description:
     lp_hit : 1 when the table contains an entry matching
              pred_pc_p0 (tag match, any confidence).
              0 on cold miss. Valid regardless of
              pred_is_loop value.
2. In loop_pred_interfaces.md, locate the upd_p0 field
   semantics section. Add lp_hit to the flat field list
   (idx, tag, way, age, conf, past_itr, curr_itr, curs,
   curs_v, victim). The existing sentence covering these
   fields states they are captured from lp_pred_t at
   predict time. lp_hit belongs in this group -- add it
   to the list in that sentence. Do not add a separate
   paragraph for lp_hit in the upd_p0 section.

### CLI-002
3. Read every field name referenced in
   loop_pred_interfaces.md that corresponds to a field
   in lp_pred_t or lp_upd_t. Compare each against the
   current struct definition in bp_structs_pkg.sv.
   Correct any name in the doc that does not match the
   struct. Document each correction found (or confirm
   no corrections needed) in Results Capture.
   Do not change the struct.

### CLI-004
4. Search the repository for all uses of lp_set outside
   of bp_structs_pkg.sv. Document the search result in
   Results Capture. If any consumer is found, stop and
   report before making any change.
5. If no consumer found: in bp_structs_pkg.sv, rename
   the lp_set field in bp_loop_meta_t to lp_idx.
   Update the comment on that field if one exists.
   Do not change any other field in bp_loop_meta_t.
   Do not change lp_pred_t or lp_upd_t.

## Constraints
- Modify bp_structs_pkg.sv and loop_pred_interfaces.md
  only.
- Do not touch loop_pred.sv, tb_loop_pred.sv, or any
  other RTL or testbench file.
- Do not change lp_pred_t or lp_upd_t field names.
  Those are addressed in BP-017b.
- Do not change any field in bp_loop_meta_t other than
  lp_set -> lp_idx.
- No sim required. This is a doc and struct-only pass.
- 80-column line limit. 2-space indent. ASCII comments
  only.
- If any consumer of lp_set is found in step 4, stop
  and report. Do not proceed with the rename.

## Deliverables
1. frontend/branch_predictor/rtl/bp_structs_pkg.sv
   (lp_set -> lp_idx in bp_loop_meta_t only)
2. planning/interfaces/loop_pred_interfaces.md
   (CLI-001 lp_hit additions, CLI-002 name corrections)
3. Results Capture filled in below
:: PROMPT:END ::
=============================================================
# Results Capture
=============================================================
:: RESULTS:START ::
## Summary
All three cleanup items completed. bp_structs_pkg.sv has
lp_set renamed to lp_idx in bp_loop_meta_t. loop_pred_
interfaces.md has lp_hit added to pred_p0 field semantics
and to the upd_p0 flat field list. CLI-002 audit found no
field name mismatches -- no corrections required.

## Test Matrix (testbench sessions only, omit otherwise)
N/A -- doc and struct cleanup, no sim.

## What was delivered
1. frontend/branch_predictor/rtl/bp_structs_pkg.sv
   - bp_loop_meta_t: lp_set renamed to lp_idx (line 152).
   - Comment unchanged ("// set index" still accurate).
   - No other fields touched.
2. planning/interfaces/loop_pred_interfaces.md
   - CLI-001a: lp_hit entry added to pred_p0 field semantics
     section, after way entry, before age entry.
   - CLI-001b: lp_hit added to flat field list in upd_p0
     field semantics section (inserted after way).
   - CLI-002: no changes required (see below).

## Test Case Results
N/A

## Assumptions made not explicit in the prompt
- The prompt states "add lp_hit after the way entry and
  before pred_is_loop." In the doc pred_is_loop appears
  before way. Interpreting the instruction as: insert lp_hit
  immediately after the way entry, which places it before
  age. This is consistent with field order in lp_pred_t
  (idx, tag, way, lp_hit, pred_is_loop, ...).
- bp_cluster.md (planning/arch) references lp_set in a
  table at line 411. This is a planning doc, not RTL.
  It is not a consumer of bp_loop_meta_t and is not
  modified by this task. Flagged here for follow-on
  cleanup if needed.

## Decisions made not explicit in the prompt
- None.

## RVA23 compliance risks and gaps noticed
- None introduced by this cleanup.

## CLI-002 Audit Results
Compared every field name in loop_pred_interfaces.md
that corresponds to lp_pred_t or lp_upd_t against the
struct definitions in bp_structs_pkg.sv.

Fields in pred_p0 section:
  pred_is_loop -- matches lp_pred_t.pred_is_loop : OK
  pred_taken   -- matches lp_pred_t.pred_taken   : OK
  idx          -- matches lp_pred_t.idx           : OK
  tag          -- matches lp_pred_t.tag           : OK
  way          -- matches lp_pred_t.way           : OK
  age          -- matches lp_pred_t.age           : OK
  conf         -- matches lp_pred_t.conf          : OK
  past_itr     -- matches lp_pred_t.past_itr      : OK
  curr_itr     -- matches lp_pred_t.curr_itr      : OK
  curs         -- matches lp_pred_t.curs          : OK
  curs_v       -- matches lp_pred_t.curs_v        : OK
  victim       -- matches lp_pred_t.victim        : OK

Fields in upd_p0 section:
  pc           -- matches lp_upd_t.pc             : OK
  actual_taken -- matches lp_upd_t.actual_taken   : OK
  pred_is_loop -- matches lp_upd_t.pred_is_loop   : OK
  pred_taken   -- matches lp_upd_t.pred_taken     : OK
  idx          -- matches lp_upd_t.idx            : OK
  tag          -- matches lp_upd_t.tag            : OK
  way          -- matches lp_upd_t.way            : OK
  age          -- matches lp_upd_t.age            : OK
  conf         -- matches lp_upd_t.conf           : OK
  past_itr     -- matches lp_upd_t.past_itr       : OK
  curr_itr     -- matches lp_upd_t.curr_itr       : OK
  curs         -- matches lp_upd_t.curs           : OK
  curs_v       -- matches lp_upd_t.curs_v         : OK
  victim       -- matches lp_upd_t.victim         : OK

Result: no CLI-002 corrections needed. Zero mismatches.

## CLI-004 Search Result
grep for lp_set across repo (all files):
  Consumers found (non-struct-definition matches):
  - planning/arch/bp_cluster.md:411 -- planning doc,
    not RTL. References lp_set in a metadata table.
    Not a struct consumer. Does not block rename.
  - prompts/BP-017a.md -- this task file. Not a consumer.
  - handoffs/PROJECT_STATUS.md -- tracking file.
  - handoffs/session_handoff-012.md -- tracking file.
  - .claude/tmp/current-prompt.md -- session file.
  - ho -- working copy of handoffs file.
  No RTL or testbench file consumes lp_set.
  Rename of lp_set -> lp_idx in bp_loop_meta_t proceeded.

## Deferred Work
- planning/arch/bp_cluster.md line 411 references lp_set.
  Should be updated to lp_idx in a follow-on doc pass.
  Outside scope of BP-017a (constraints limit changes to
  bp_structs_pkg.sv and loop_pred_interfaces.md only).

## Other Notes
- No sim performed. This is a doc and struct-only pass
  as specified.

## Files Modified
Not captured

:: RESULTS:END ::

