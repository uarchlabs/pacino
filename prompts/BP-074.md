<!-- SPDX-License-Identifier: Apache-2.0                       -->
<!-- Copyright (c) 2026 Jeff Nye, uarchlabs.com                -->
<!-- SPDX-FileCopyrightText: 2026 Jeff Nye <jeff@uarchlabs.com -->
=============================================================
# Task Header 
=============================================================
:: HEADER:START ::
| Field        | Value                   | Notes                    |
|--------------|-------------------------|--------------------------|
| Task ID      | BP-074                  | renumber if needed       |
| Date         | 2026.06.26              |                          |
| Module       | bp_history              |                          |
| Run time     | 6m.53s                  |                          |
| Ctx %        | 14%                     |                          |
| Model        | claude-opus-4-8 normal  |                          |
| Resume sha   | 19ce1f46-1a4d-4263-938a-d4ed0180f2bf |             |
| PA session   | 055                     |                          |
Task:   [ ] experiment  [ ] implementation  [ ] debug
        [ ] cleanup     [ ] testbench       [x] verification
Mode:   [x] automated   [ ] manual
Status: [ ] in-progress [x] complete        [ ] abandoned

# Task Overview
Consistency-verify the section 6 canonical Fold Definition added to
bp_history_decisions.md against the RTL and the BP-073 anchors, and
confirm the doc edit is isolated (no RTL/tb/package/Makefile change
snuck in alongside it). The doc rewording is the only repo change
since tag bp_history-bp073-complete; this task proves the reworded
authority describes the as-built RTL, that its worked example and
the BP-073 literals agree, that its internal cross-references resolve
after the section renumber, and that the full bpu suite is still
green.
:: HEADER:END ::
=============================================================
:: DISCUSSION:START ::
=============================================================
# Results Discussion 
## Claude.code Console Output


  BP-074 — Result: COMPLETE (suite green, doc edit isolated)

  Overall: section 6 is consistent with the as-built RTL and the BP-073 anchors;
  the doc edit is isolated; the suite is green.

  All six requirements passed:

  1. Repo drift — Only planning/arch/bp_history_decisions.md changed vs tag
  bp_history-bp073-complete (+214/-93). bp_history.sv, tb_bp_history.sv, both
  packages, and the Makefile are byte-identical to the tag (scoped git diff
  empty). Untracked stray files exist (BP-072.md, bpu/, notes, .swp, etc.) but
  none are in-scope tracked files and none modify the audited files.
  2. Cross-references — All 18 distinct section N/N.M references resolve to
  existing sections with matching titles; the 6-10 → 7-11 renumber is clean
  (e.g. "section 8" → "section 9" forward ref moved correctly). One non-blocking
  observation flagged: section 10 calls bp_cluster.md "the cluster is the
  pointer authority, section 2" — the number resolves correctly, but the prose
  is stale vs section 2's ratified module-owned decision. It's a git context
  line (pre-existing, untouched by this edit), so not a renumber defect —
  reported, not self-healed.
  3. Doc-RTL geometry — 6.1/6.2/6.3 match fold_ghr/fold_step bit-for-bit, cited
  by line (e.g. pos=(i+W-1)%W at line 123; f[W-1]^=new_bit at 156;
  wrap_pos=(H-1)%W at 158).
  4. Worked-example re-derivation — From section 6 rules alone: TC14 → 0xE5,
  TC15 → 0x9, TC16 → 0xC000_0000_0000_0000. Each equals the committed tb literal
  and the DUT output (sim_history: 3/3 anchors, 19224 golden comparisons, ALL
  PASSED).
  5. Interface doc — bp_history_interfaces.md Checkpoint Timing reads
  POST-advance, matching decisions.md section 7.
  6. Regression — 33/33 bpu targets green this session (v5.048): 13 lint @ 0
  warn/0 err, 14 sim PASS, 6 cov complete. No waiver invoked.

  Per CLAUDE.md I wrote only within the RESULTS markers plus the Model header
  field; the header Status checkbox was left untouched (Status COMPLETE is
  stated in the Summary). No RTL/tb/package/Makefile/planning-doc was modified.
  Only prompts/BP-074.md changed.

  One item for your attention: the section-10 "cluster is the pointer authority"
  wording contradicts the module-owned ruling in section 2 — recommend a
  separate human-reviewed fix to that planning doc (out of this task's read-only
  scope).


## My Assessment

I made the doc edit mentioned by the IA. This was supplied by PA:

  bp_cluster.md   -- the cluster reads the exposed pointer for FTQ
                     entry construction and supplies rollback
                     stimulus (the checkpoint index); it does NOT
                     own or drive the pointer. Pointer ownership is
                     module-internal (section 2). Also: rollback
                     stimulus path, FTQ entry contents, GHR 256b /
                     PHR 32b, folds recomputed on rollback.


## Claude.ai Assessment

Clean pass, and this is the one in the whole chain you can take at face value — because its job was to be checkable and it was checked. Drop this in the Claude.ai Assessment block.

BP-074 did the three things that mattered and did them in a verifiable way. The repo-drift check is concrete: scoped git diff against the tag is empty for the RTL, tb, packages, and Makefile, so "the doc edit is isolated, nothing snuck into the code" is now evidence rather than assertion. The cross-reference audit cleared my renumbering — all 18 references resolve, the 6-10 → 7-11 shift moved every forward reference correctly — which is exactly the part I most wanted a second pair of eyes on, since the renumber was my hand. And requirement 4 is the real close: the three anchors were re-derived from §6's stated rules alone (age order → posmap → XOR) and reproduce the committed literals and the DUT output. I checked TC14's derivation independently — ages {0,1,3,6,7}, posmap(i)=(i+7)%8 → positions {7,6,5,2,0} → 0xE5 — and it's right. That's the loop genuinely closed: §6 spec ⇒ derived value ⇒ committed literal ⇒ DUT, all four equal. A posmap typo in the doc would have broken the derivation; it didn't.

The req-3 line-cited correspondence (fold_ghr pos=(i+W-1)%W at 123, f[W-1]^=new_bit at 156, wrap_pos=(H-1)%W at 158) ties the words to the code at the bit level, and req 5 confirms interfaces.md actually reads post-advance — so §7's "matches the interface doc" claim is now verified, not just stated. 33/33 green, no waiver. And the §10 wording issue it surfaced is already handled — your supplied reword is in the discussion block, so that's closed.

One genuinely useful catch worth noting, not a defect: the IA reported it ran the v5.048 verilator from tools/bin rather than the v5.020 on PATH. That's the correct tool per the project's Vars.mk, but it's the first time a results file has explicitly called out the version-selection — worth confirming once that tools/bin is what your other recent sessions used too, so the counts are all from the same toolchain. Almost certainly yes (it's the documented common install), but it's the kind of thing that's cheap to confirm and annoying to discover later.

Process: this is the corrected pattern fully landed. It stopped at the Status checkbox, stated complete in Summary, didn't self-heal the §10 wording it found, ran every target including the non-all ones, and flagged the §6.6 sha placeholders in Deferred Work so they don't get forgotten. Nothing to dock.

## Follow-on Actions
Nothing required

## CLAUDE.md Updates
Nothing required

## Other Planning File Updates
Nothing required
:: DISCUSSION:END ::
=============================================================
:: PROMPT:START ::
=============================================================

## Task ID
BP-074

## Context Loaded
@rtl/core/frontend/bpu/tb/tb_bp_history.sv
@rtl/core/frontend/bpu/rtl/bp_history.sv
@planning/arch/bp_history_decisions.md
@planning/interfaces/bp_history_interfaces.md
@rtl/core/frontend/bpu/rtl/bp_defines_pkg.sv
@rtl/core/frontend/bpu/rtl/bp_structs_pkg.sv
@rtl/core/frontend/bpu/Makefile

## Hypothesis
The section 6 canonical Fold Definition in bp_history_decisions.md is
internally consistent, matches the as-built fold_ghr / fold_step in
bp_history.sv bit-for-bit, and its worked example plus the BP-073
anchors (TC14-TC16) agree. The only repo change since tag
bp_history-bp073-complete is bp_history_decisions.md (plus task
files). The full bpu suite is still green. If any of these fail, the
mismatch is the finding.

## Background
Session-055 captured the fold geometry into bp_history_decisions.md
section 6 as the project's own authority (age indexing, position
mapping posmap(i) = (i+W-1)%W, window eviction at (H-1)%W, the
recompute == incremental invariant, and a worked example tied to the
BP-073 TC14 anchor: history 0xD3 -> fold 0xE5). Xiangshan was demoted
to an origin footnote (section 6.6). Old sections 6-10 were
renumbered to 7-11 and the "section N>=6" cross-references updated.

That edit was doc-only: no RTL, testbench, package, or Makefile
change was intended. bp_history.sv carries the BP-072 merged
module-owned-pointer + corrected-geometry RTL; tb_bp_history.sv
carries the BP-072 equivalence suite (TC1-TC13) and the BP-073
external anchors (TC14-TC16). Both are read-only here.

This task is a consistency audit plus a regression run. It does NOT
modify the RTL, the packages, the Makefile, or the planning docs.
If the audit finds a divergence -- a doc statement that the RTL does
not implement, a worked-example value that does not match the
committed literal, a dangling cross-reference from the renumber, or
an interface-doc disagreement -- the task REPORTS it and sets Status
in-progress. It does not edit the doc or the RTL to make them agree;
that correction is a separate, human-reviewed change. (This is the
same do-not-self-heal rule as BP-073 requirement 4: the check exists
to catch a mismatch, so it must not be allowed to erase one.)

## Binding Previous Decisions
- bp_history_decisions.md section 6 is the fold authority. The RTL
  is measured against it, not the reverse. Do not modify section 6.
- bp_history.sv, the packages, and the Makefile are read-only. No
  RTL, package, or Makefile change is expected or permitted by this
  task; detecting an unexpected one is part of the job (requirement
  1), not a license to make one.
- The anchors already exist (BP-073 TC14-TC16). This task does not
  add tests; it audits the doc against the existing RTL and tests
  and re-runs the suite. A new check may be added only if needed to
  expose a mismatch the existing tests do not, and only in
  tb_bp_history.sv; report before adding.
- recompute == incremental and the three external anchors were
  already proven in simulation (BP-072 / BP-073). This task confirms
  the DOCUMENT now correctly describes that proven behavior.

## Specific Requirements
Read bp_history_decisions.md (section 6 and the renumbered sections
7-11), bp_history.sv, tb_bp_history.sv, and bp_history_interfaces.md
before reporting.

1. Repo-drift check. Establish that the only working-tree/committed
   change since tag bp_history-bp073-complete is
   bp_history_decisions.md (and task/prompt files under prompts/).
   Use git (e.g. git diff --stat bp_history-bp073-complete, git
   status) and report the exact file list. Confirm NO change to
   bp_history.sv, tb_bp_history.sv, the packages, or the Makefile.
   If any of those changed, that is a finding: report the diff and
   set Status in-progress.

2. Doc-internal cross-reference resolution. After the section 6
   insertion and the 6-10 -> 7-11 renumber, verify every "section N"
   and "section N.M" reference in bp_history_decisions.md resolves
   to an existing section whose title matches the intent (e.g. a
   "(section 7)" meant as Checkpoint points at section 7 Checkpoint,
   "(section 9)" meant as Open Items points at section 9). Report
   any reference that dangles, points at the wrong section, or was
   missed by the renumber.

3. Doc-RTL geometry consistency. Place section 6 beside the RTL fold
   functions and confirm they implement the SAME geometry:
   - 6.1 age indexing: age 0 = newest; the RTL buffer-walk
     (anchor - i for recompute; write_addr - H for the eviction
     fetch) realizes that age order.
   - 6.2 posmap(i) = (i+W-1)%W with newest at W-1: matches fold_ghr
     (pos = (i+W-1)%W) and the high-end insertion in fold_step
     (f[W-1] ^= new_bit after the rotate).
   - 6.3 eviction at (H-1)%W: matches fold_step (wrap_pos =
     (H-1)%W; f[wrap_pos] ^= bit_out) and the leaving-bit address.
   State the correspondence explicitly, function and line. Report
   any place the words and the code diverge.

4. Worked-example re-derivation (the real check, not a re-run).
   Independently recompute, from section 6's stated rules only (age
   order -> posmap -> XOR), the expected fold for each BP-073 anchor
   history:
   - TC14 TAGE T1, H=W=8, history 0xD3 -> expect 0xE5 (the section
     6.5 example value);
   - TC15 ITTAGE IT1, H=W=4, 2-bit sub-window -> expect 0x9;
   - TC16 SC ST3, H=W=64, wrapped -> expect 0xC000_0000_0000_0000.
   Confirm each derived value equals (a) the value stated in the
   doc / committed as the tb literal and (b) the DUT output observed
   in sim_history. If a derivation from the doc's rules does not
   reproduce the committed literal, the DOC is wrong (e.g. a posmap
   typo) -- report it; do not change the doc, set Status in-progress.

5. Interface-doc agreement. Confirm bp_history_interfaces.md
   Checkpoint Timing reads POST-advance, consistent with
   bp_history_decisions.md section 7's claim that the two match.
   Report if it does not (a doc-doc inconsistency to flag, not to
   silently fix).

6. Regression. Run every bpu Makefile target (all lint_*, all
   sim_*/sim, all cov_* incl. cov_history and cov_bpu) and report
   pass/fail (sim/cov) and warning/error count (lint) for each, from
   this session. The suite must be green (the doc edit must not have
   perturbed anything; if a target is non-green, that itself is the
   finding).

## Constraints
- Read-only on bp_history.sv, the packages, the Makefile, and both
  planning docs. This task writes only its Results Capture (and a
  tb_bp_history.sv check only if requirement-3/4 needs one to expose
  a mismatch -- report before adding).
- No silent reconciliation. A doc-RTL mismatch, a wrong/dangling
  cross-reference, a worked-example value that the doc's own rules do
  not reproduce, or an interface-doc disagreement is REPORTED and
  sets Status in-progress. Do not edit any planning doc or the RTL to
  make them agree.
- No waivers. Any non-green bpu target blocks Status: complete.
- Standard bpu sim flags apply. Do not add new suppressions; if one
  appears needed, report it.

## Deliverables
- In Results Capture:
  - the repo-drift file list and an explicit statement that
    bp_history.sv / tb_bp_history.sv / packages / Makefile are
    unchanged since bp_history-bp073-complete (req 1);
  - the cross-reference resolution result, listing any bad reference
    or stating all resolve (req 2);
  - the section-6-to-RTL correspondence (req 3), function and line;
  - the three independent re-derivations from section 6's rules and
    confirmation each equals the committed literal and the DUT output
    (req 4), or the mismatch found;
  - the interface-doc Checkpoint Timing confirmation (req 5);
  - the full per-target pass/fail and lint warning/error counts
    (req 6).
- An explicit overall statement: either "section 6 is consistent
  with the as-built RTL and the BP-073 anchors; doc edit isolated;
  suite green" (Status complete) or the specific mismatch (Status
  in-progress).
- Results Capture written into ./prompts/BP-074.md, every section
  completed including the Test Matrix (or "omitted -- no new tests"
  if none were added). Populate the Model header per CLAUDE.md.
- Note: no new module. No RTL, package, Makefile, or planning-doc
  change. tb_bp_history.sv change only if a check is needed to expose
  a mismatch (report first).

:: PROMPT:END ::
=============================================================
:: RESULTS:START ::
=============================================================
## Summary
OVERALL: section 6 is consistent with the as-built RTL and the
BP-073 anchors; the doc edit is isolated; the suite is green.
Status: COMPLETE.

Consistency audit of the section 6 canonical Fold Definition added
to bp_history_decisions.md, plus a full bpu regression. All six
requirements pass:
  1. Repo drift: only bp_history_decisions.md changed vs tag
     bp_history-bp073-complete. RTL/tb/packages/Makefile unchanged.
  2. Cross-references: every "section N"/"section N.M" resolves to
     an existing section with a matching title. Renumber is clean.
     One pre-existing wording tension flagged (section 10, see
     below) -- non-blocking, reference number resolves correctly.
  3. Doc-RTL geometry: 6.1/6.2/6.3 match fold_ghr / fold_step
     bit-for-bit (function and line cited below).
  4. Worked example: all three anchor folds re-derived from section
     6 rules alone reproduce the committed tb literals AND the DUT
     output (0xE5, 0x9, 0xC000_0000_0000_0000).
  5. Interface doc: bp_history_interfaces.md Checkpoint Timing reads
     POST-advance, matching decisions.md section 7.
  6. Regression: 33/33 bpu targets green this session (13 lint @
     0 warn/0 err, 14 sim PASS, 6 cov complete).

## Test Matrix (testbench sessions only, omit otherwise)
Omitted -- no new tests. This is a verification/audit task; no
tb_bp_history.sv check was needed to expose a mismatch (none found).
The existing TC14-TC16 anchors already cover requirement 4.

## What was delivered
- Requirement 1 -- Repo-drift check.
  git diff --stat bp_history-bp073-complete (tracked): the ONLY
  changed tracked file is
    planning/arch/bp_history_decisions.md  (+214 / -93)
  git diff --stat scoped to the in-scope files returns EMPTY, i.e.
  byte-identical to the tag:
    rtl/core/frontend/bpu/rtl/bp_history.sv        UNCHANGED
    rtl/core/frontend/bpu/tb/tb_bp_history.sv      UNCHANGED
    rtl/core/frontend/bpu/rtl/bp_defines_pkg.sv    UNCHANGED
    rtl/core/frontend/bpu/rtl/bp_structs_pkg.sv    UNCHANGED
    rtl/core/frontend/bpu/Makefile                 UNCHANGED
  Working tree also shows decisions.md with an uncommitted " M"
  (the section-6 capture under audit) and these UNTRACKED stray
  files, none of which are in-scope tracked files and none of which
  modify the audited RTL/tb/packages/Makefile:
    prompts/BP-074.md (this task file), BP-072.md, context-report.md,
    get_ras.sh, ho, notes, bpu/ (dir),
    planning/arch/.bp_history_decisions.md.swp (editor swap)
  Conclusion: the doc edit is isolated; no RTL/tb/package/Makefile
  change snuck in. PASS.

- Requirement 2 -- Cross-reference resolution.
  All distinct references enumerated and resolved:
    section 2   -> Pointer Model: module-owned         OK
    section 2.1 -> Advance                              OK
    section 2.2 -> Rollback by index                    OK
    section 2.3 -> Outputs for FTQ visibility           OK
    section 3   -> Prediction Update: dual-slot         OK
    section 3.3 -> Combined fold ordering               OK
    section 3.4 -> Checkpoint granularity               OK
    section 3.5 -> RTL fix if/else-if                   OK
    section 4   -> Rollback Priority                    OK
    section 5   -> Folded History After Rollback        OK
    section 5.3 -> Decoupling from G15                  OK
    section 5.4 -> Performance cost                     OK
    section 6   -> Fold Definition (Canonical)          OK
    section 6.4 -> Equivalence invariant                OK
    section 6.6 -> Origin                               OK
    section 7   -> Checkpoint                           OK
    section 8   -> Parameters                           OK
    section 9   -> Open Items                           OK
  The 6-10 -> 7-11 renumber is clean: the only forward references
  that had to move did move, e.g. section 10 now reads "port
  reconciliation listed in section 9" (was "section 8"), and the
  Document History / Interactions / Checkpoint headings renumbered
  consistently. No reference dangles or points at the wrong section.
  OBSERVATION (non-blocking, pre-existing): section 10 (Interactions)
  describes bp_cluster.md as "pointer ownership (the cluster is the
  pointer authority, section 2)". The reference NUMBER resolves
  correctly (section 2 is the pointer-ownership section), so this is
  not a cross-reference/renumber defect and does not gate this task.
  However the parenthetical prose "the cluster is the pointer
  authority" is stale: section 2 ratifies the pointer as
  MODULE-owned (the cluster does not drive it). This line is a git
  CONTEXT line, unchanged by the section-6 capture (it predates this
  edit). Recommend a separate, human-reviewed wording fix; not
  touched here per the do-not-self-heal rule. PASS (all references
  resolve).

- Requirement 3 -- Doc-RTL geometry correspondence (function:line in
  rtl/core/frontend/bpu/rtl/bp_history.sv).
  - 6.1 age indexing (age 0 = newest, downward buffer walk):
      fold_ghr line 122: bit_idx = (ptr_in - i + GHR_WIDTH) %
        GHR_WIDTH  -> offset i=0 reads the anchor (newest), i grows
        into older bits (anchor - i).
      recompute anchor = ckpt - 1: line 206 rb_anchor =
        (int'(rb_gptr) - 1 + GHR_WIDTH) % GHR_WIDTH.
      eviction fetch at write_addr - H: normal path lines 298, 311,
        ... ghr_mem[(int'(ghist_ptr_r) - <H> + GHR_WIDTH) %
        GHR_WIDTH]. MATCH.
  - 6.2 posmap(i) = (i + W - 1) % W, newest at W-1:
      fold_ghr line 123: pos = (i + W - 1) % W.
      fold_step rotate line 154: f = ((f<<1) | (f>>(W-1))) & mask;
        insertion line 156: f[W-1] = f[W-1] ^ new_bit. MATCH.
  - 6.3 eviction at (H-1) % W:
      fold_step line 158: wrap_pos = (H - 1) % W;
      line 159: f[wrap_pos] = f[wrap_pos] ^ bit_out. MATCH.
  No divergence between the section-6 words and the code. PASS.

- Requirement 4 -- Worked-example re-derivation from section 6 rules
  only (age order -> posmap(i)=(i+W-1)%W -> XOR), independent of the
  DUT:
  - TC14 TAGE T1, H=W=8, history 0xD3. Age bits (age0=newest):
    set ages {0,1,3,6,7}. posmap(i)=(i+7)%8 ->
    {7, 0, 2, 5, 6} -> 1110_0101 = 0xE5.
    Equals section 6.5 example (0xE5), tb literal (64'h..E5), and
    DUT (sim_history "TC14 pass ... = 0xE5"). MATCH.
  - TC15 ITTAGE IT1, H=W=4, 2-bit sub-window. Set ages {0,1}.
    posmap(i)=(i+3)%4 -> {3, 0} -> 1001 = 0x9.
    Equals tb literal (0x9) and DUT (TC15 pass). MATCH.
  - TC16 SC ST3, H=W=64, wrapped. Set ages {0,63}.
    posmap(i)=(i+63)%64 -> {63, 62} -> 0xC000_0000_0000_0000.
    Equals tb literal and DUT (TC16 pass). MATCH.
  sim_history reports 3/3 external anchor checks passed and "ALL
  EXTERNAL ANCHORS MATCH". The doc's rules reproduce every committed
  literal. PASS.

- Requirement 5 -- Interface-doc agreement.
  bp_history_interfaces.md, Checkpoint Interface / Timing: "The
  checkpoint captures the post-advance pointer values for the bundle
  (the pointer after this cycle's prediction advance)." This is
  POST-advance, consistent with decisions.md section 7, which states
  the timing is POST-advance and "matches bp_history_interfaces.md
  (Checkpoint Timing)". No doc-doc disagreement. PASS.

- Requirement 6 -- Regression (this session, tools/bin/verilator
  v5.048). Every bpu Makefile target run; all green.

## Test Case Results
Per-target results (this session). lint: warn/err counts; sim/cov:
exit status + self-check summary.

LINT (13 targets, all 0 warning / 0 error):
  lint                 0/0
  lint_loop_pred       0/0
  lint_tage_table      0/0
  lint_ittage_table    0/0
  lint_ittage_cntrl    0/0
  lint_ittage          0/0
  lint_tage_cntrl      0/0
  lint_tage            0/0
  lint_ras             0/0
  lint_ftb_array       0/0
  lint_ftb_plru        0/0
  lint_ftb_cntrl       0/0
  lint_ftb             0/0

SIM (14 targets, all rc=0 PASS):
  sim                  PASS  (BP-001: 16 checks passed)
  sim_history          PASS  (16 directed cases, 19224 golden fold
                              comparisons, 3 external anchors;
                              ALL TESTS PASSED / ANCHORS MATCH)
  sim_ubtb             PASS  (ALL TESTS PASSED)
  sim_loop_pred        PASS  (ALL TC1-TC13 TESTS PASSED)
  sim_tage_table       PASS  (TESTBENCH PASSED)
  sim_ittage_table     PASS  (PASS: 32  FAIL: 0)
  sim_ittage_cntrl     PASS  (RESULTS: 147 PASS, 0 FAIL)
  sim_ittage           PASS  (all 211 checks passed)
  sim_ras              PASS  (TC pins incl. TD #78; $finish)
  sim_ftb              PASS  (pos round-trip checks; $finish)
  sim_tage             PASS  (BP-061: all tests passed)
  sim_tage_fast        PASS  (BP-061: all tests passed)
  sim_tage_tasks       PASS  (TB-001: all tests passed)
  sim_tage_manual      PASS  (tage_round_trip_sanity : PASS)

COV (6 targets, all rc=0, coverage emitted):
  cov_tage             PASS
  cov_tage_table       PASS
  cov_loop_pred        PASS
  cov_history          PASS  (history HTML emitted)
  cov_ubtb             PASS
  cov_bpu              PASS  (bpu merged HTML emitted)

TOTAL: 33/33 green. No non-green target. No waiver invoked.

## Assumptions made not explicit in the prompt
- "Every bpu Makefile target" was taken to mean all 13 lint, all 14
  sim (including non-all targets sim_ittage, sim_tage_manual), and
  all 6 cov targets, run from a clean tree this session. cov_bpu was
  run (which re-runs its five cov prerequisites and the merge).
- Pass criterion: the project testbenches are self-checking and
  $fatal on error (non-zero exit); a target with exit 0 and its own
  PASS summary is counted green. Lint green = exit 0 with zero
  %Warning / %Error after the project-standard suppressions already
  in the Makefile (no new suppression added).

## Decisions made not explicit in the prompt
- The section-10 "cluster is the pointer authority" wording tension
  was REPORTED (requirement 2 observation), not edited, per the
  do-not-self-heal rule and the read-only constraint on planning
  docs. It does not block: the section-2 reference number resolves
  to the correct section and the line was not introduced or moved by
  the audited edit.
- The header Status checkbox was left untouched: CLAUDE.md restricts
  writes to within the RESULTS markers plus the Model header field
  only. The explicit Status (COMPLETE) is stated in this Summary.

## RVA23 compliance risks and gaps noticed
None new. This task is a doc/RTL consistency audit of the fold
geometry; it touches no ISA-visible behavior. Pre-existing deferred
items (HI2 PHR-in-fold, HI5 checkpoint reclaim) are documented in
decisions.md section 9 and are out of scope here.

## Deferred Work
- Recommend a separate, human-reviewed edit to decisions.md
  section 10 to reword "the cluster is the pointer authority" so it
  no longer contradicts the ratified module-owned pointer decision
  (section 2). Pre-existing; outside this audit's read-only scope.
- decisions.md section 6.6 still carries <XS-COMMIT-SHA> /
  <XS-CAPTURE-DATE> placeholders for the Xiangshan origin citation
  (informational only; the section is the authority, not a live
  dependency).

## Other Notes
- Verilator used: $(RVA_ROOT)/tools/bin/verilator v5.048 (per
  rtl/Vars.mk), not the v5.020 on PATH.
- No tb_bp_history.sv change was required; the existing TC14-TC16
  anchors and the TC1-TC13 equivalence suite already expose any
  geometry mismatch, and none was found.

## Files Modified
- prompts/BP-074.md
:: RESULTS:END ::

