=============================================================
# Task Header
=============================================================
:: HEADER:START ::
| Field        | Value                   | Notes                    |
|--------------|-------------------------|--------------------------|
| Task ID      | BP-017b                 |                          |
| Date         | 2026.04.09              |                          |
| Module       | loop_pred.sv            |                          |
| Run time     | 11m.7s                  |                          |
| Ctx %        | 55%                     |                          |
| Model        | Sonnet 4.6 medium       |                          |
| Resume sha   | 7455181a-2806-4f02-a77c-92424ac7260d |             |
Task:   [ ] experiment  [ ] implementation  [ ] debug       [x] cleanup [ ] testbench
Mode:   [x] automated   [ ] manual
Status: [ ] in-progress [x] complete        [ ] abandoned
# Overview of task
Cleanup pass: CLI-008 and CLI-011.
CLI-008: apply consistent lp_ prefix to all unprefixed
  fields in lp_pred_t and lp_upd_t in bp_structs_pkg.sv.
CLI-011: update loop_pred.sv and tb_loop_pred.sv to use
  the renamed field names. Update loop_pred_interfaces.md
  field name references to match.
Sim run required: all 12 existing tests must pass.
:: HEADER:END ::
=============================================================
# Paste c.code console output and c.ai discussion
=============================================================
:: DISCUSSION:START ::
# Results Discussion
## Claude.code Console Output

  All deliverables complete:

  1. bp_structs_pkg.sv — lp_pred_t and lp_upd_t fields renamed with lp_ prefix  
  (12 fields each). lp_hit, pc, target, actual_taken unchanged. bp_loop_meta_t
  untouched.                                                                    
  2. loop_pred.sv — All pred_comb.* and upd_p0.* field accesses updated. No
  logic changes.                                                                
  3. tb_loop_pred.sv — All pred_p0.* and upd_p0.* field accesses updated. No
  test logic changes.                                                           
  4. loop_pred_interfaces.md — Field labels and inline references updated. (A
  double-replacement artifact from ordered replace_all was caught and           
  corrected.)                                  
                                                                                
  Lint: Zero warnings (-Wall -Wno-UNUSED -Wno-IMPORTSTAR -Wno-VARHIDDEN         
  --timing)
                                                                                
  Sim: TC1–TC13: all PASS (ALL TC1-TC13 TESTS PASSED)   

## My Assessment

GTG.

## Claude.ai Assessment

Clean pass. 13/13, lint clean, all four files updated correctly.
One thing worth noting from the results: the double-replacement artifact (lp_lp_pred_is_loop) in the interface doc was caught and corrected within the session. This is a known risk with ordered string replacement on names that share substrings — the explicit rename table in the prompt helped Claude Code catch it. Good outcome.
CLI-008 and CLI-011 are now closed.

## Follow-on Actions
- [x] Update PROJECT_STATUS.md: close CLI-008, CLI-011.
- [x] Update bp_cluster.md:411 lp_set -> lp_idx (manual,
      flagged in BP-017a deferred work).
## CLAUDE.md Updates
None
## Other Planning File Updates
complete
:: DISCUSSION:END ::
=============================================================
# Claude.code Prompt
=============================================================
:: PROMPT:START ::
## Task ID
BP-017b

## Context Loaded
@frontend/branch_predictor/rtl/bp_structs_pkg.sv
@frontend/branch_predictor/rtl/loop_pred.sv
@frontend/branch_predictor/tb/tb_loop_pred.sv
@planning/interfaces/loop_pred_interfaces.md
@prompts/BP-017b.md

## Hypothesis
lp_pred_t and lp_upd_t fields lack consistent lp_ prefix.
loop_pred.sv and tb_loop_pred.sv reference the old
unprefixed names. Applying lp_ prefix uniformly and
updating all consumers closes CLI-008 and CLI-011.

## Background

CLI-008: lp_pred_t in bp_structs_pkg.sv has fields with
mixed naming. Most fields lack the lp_ prefix that
bp_loop_meta_t uses consistently. The same issue exists
in lp_upd_t, which mirrors lp_pred_t fields flat.

CLI-011: loop_pred.sv uses the current unprefixed field
names throughout (pred_comb.idx, pred_comb.tag,
upd_p0.conf, upd_p0.curr_itr, etc.). tb_loop_pred.sv
does the same (pred_p0.conf, pred_p0.curr_itr,
upd_p0.idx, etc.). Both files must be updated to use
the new lp_-prefixed names after the struct change.

## Binding Previous Decisions
- lp_hit is already prefixed -- no change.
- pc, target, actual_taken in lp_upd_t are not
  loop-predictor-specific -- no lp_ prefix applied.
- bp_loop_meta_t is not modified. lp_pst_itr and
  lp_cur_itr abbreviations in bp_loop_meta_t are
  intentional and settled. Do not touch bp_loop_meta_t.
- loop_pred_interfaces.md field name references must
  be updated to match the new struct field names.
  The struct is the authority.

## Specific Requirements

### CLI-008 -- bp_structs_pkg.sv

Apply lp_ prefix to the following fields in lp_pred_t:
  idx          -> lp_idx
  tag          -> lp_tag
  way          -> lp_way
  pred_is_loop -> lp_pred_is_loop
  pred_taken   -> lp_pred_taken
  age          -> lp_age
  conf         -> lp_conf
  past_itr     -> lp_past_itr
  curr_itr     -> lp_curr_itr
  curs         -> lp_curs
  curs_v       -> lp_curs_v
  victim       -> lp_victim
  lp_hit       -- already correct, no change

Apply the same renames to lp_upd_t for the fields that
mirror lp_pred_t. The additional lp_upd_t fields pc,
target, actual_taken are not renamed.

Do not touch any other struct in bp_structs_pkg.sv.
Do not touch bp_loop_meta_t.

### CLI-011 -- loop_pred.sv

Update every reference to the renamed fields throughout
loop_pred.sv. The module port types (lp_pred_t,
lp_upd_t) do not change -- only field accesses change.

Key references to update (not exhaustive -- find all):
  pred_comb.idx          -> pred_comb.lp_idx
  pred_comb.tag          -> pred_comb.lp_tag
  pred_comb.way          -> pred_comb.lp_way
  pred_comb.pred_is_loop -> pred_comb.lp_pred_is_loop
  pred_comb.pred_taken   -> pred_comb.lp_pred_taken
  pred_comb.age          -> pred_comb.lp_age
  pred_comb.conf         -> pred_comb.lp_conf
  pred_comb.past_itr     -> pred_comb.lp_past_itr
  pred_comb.curr_itr     -> pred_comb.lp_curr_itr
  pred_comb.curs         -> pred_comb.lp_curs
  pred_comb.curs_v       -> pred_comb.lp_curs_v
  pred_comb.victim       -> pred_comb.lp_victim
  upd_p0.idx             -> upd_p0.lp_idx
  upd_p0.tag             -> upd_p0.lp_tag
  upd_p0.way             -> upd_p0.lp_way
  upd_p0.pred_is_loop    -> upd_p0.lp_pred_is_loop
  upd_p0.pred_taken      -> upd_p0.lp_pred_taken
  upd_p0.age             -> upd_p0.lp_age
  upd_p0.conf            -> upd_p0.lp_conf
  upd_p0.past_itr        -> upd_p0.lp_past_itr
  upd_p0.curr_itr        -> upd_p0.lp_curr_itr
  upd_p0.curs            -> upd_p0.lp_curs
  upd_p0.curs_v          -> upd_p0.lp_curs_v
  upd_p0.victim          -> upd_p0.lp_victim

### CLI-011 -- tb_loop_pred.sv

Apply the same field renames throughout tb_loop_pred.sv.
Key references include but are not limited to:
  pred_p0.lp_hit, pred_p0.lp_pred_is_loop,
  pred_p0.lp_pred_taken, pred_p0.lp_way,
  pred_p0.lp_conf, pred_p0.lp_past_itr,
  pred_p0.lp_curr_itr, pred_p0.lp_age,
  pred_p0.lp_victim, upd_p0.lp_idx, upd_p0.lp_tag,
  upd_p0.lp_way, upd_p0.lp_hit, upd_p0.lp_pred_is_loop,
  upd_p0.lp_pred_taken, upd_p0.lp_conf,
  upd_p0.lp_past_itr, upd_p0.lp_curr_itr,
  upd_p0.lp_curs, upd_p0.lp_curs_v, upd_p0.lp_victim.

Also update alloc_entry task field assignments:
  upd_p0.lp_hit, upd_p0.lp_pred_is_loop,
  upd_p0.lp_idx, upd_p0.lp_tag, upd_p0.lp_victim.

### CLI-011 -- loop_pred_interfaces.md

Update all field name references in the pred_p0 and
upd_p0 field semantics sections to use the new lp_-
prefixed names. The field descriptions do not change,
only the field name labels.

### Lint check

After all changes, run the Verilator lint target for
loop_pred. Zero warnings expected (other than
-Wno-DECLFILENAME and project-standard suppressions).
Document lint output in Results Capture.

## Constraints
- Modify bp_structs_pkg.sv, loop_pred.sv, tb_loop_pred.sv,
  and loop_pred_interfaces.md only.
- Do not modify bp_loop_meta_t in bp_structs_pkg.sv.
- Do not modify any other struct in bp_structs_pkg.sv.
- Do not modify any test logic -- only field name updates.
- All 12 existing tests must pass after rename.
- Verilator 5.020 clean: -Wall -Wno-DECLFILENAME
  -Wno-IMPORTSTAR -Wno-UNUSED --timing
- 80-column line limit. 2-space indent. ASCII comments.
- If any unexpected consumer of the old field names is
  found outside the four listed files, stop and report
  before proceeding.

## Deliverables
1. frontend/branch_predictor/rtl/bp_structs_pkg.sv
   (lp_pred_t and lp_upd_t field renames only)
2. frontend/branch_predictor/rtl/loop_pred.sv
   (field name updates only -- no logic changes)
3. frontend/branch_predictor/tb/tb_loop_pred.sv
   (field name updates only -- no logic changes)
4. planning/interfaces/loop_pred_interfaces.md
   (field name label updates only)
5. Lint output showing zero warnings
6. Sim run showing all 12 tests PASS
7. Results Capture filled in below
:: PROMPT:END ::
=============================================================
# Results Capture
=============================================================
:: RESULTS:START ::
## Summary
All four files updated. lp_pred_t and lp_upd_t fields in
bp_structs_pkg.sv carry the lp_ prefix consistently.
loop_pred.sv and tb_loop_pred.sv reference the renamed
fields throughout. loop_pred_interfaces.md field labels
and inline references updated to match. Lint clean, all
13 tests pass.

## Test Matrix (testbench sessions only, omit otherwise)
N/A -- rename only, no new test logic.

## What was delivered
1. frontend/branch_predictor/rtl/bp_structs_pkg.sv
   lp_pred_t and lp_upd_t field renames:
   idx->lp_idx, tag->lp_tag, way->lp_way,
   pred_is_loop->lp_pred_is_loop,
   pred_taken->lp_pred_taken, age->lp_age, conf->lp_conf,
   past_itr->lp_past_itr, curr_itr->lp_curr_itr,
   curs->lp_curs, curs_v->lp_curs_v, victim->lp_victim.
   lp_hit unchanged. pc, target, actual_taken in lp_upd_t
   unchanged. bp_loop_meta_t not touched.
2. frontend/branch_predictor/rtl/loop_pred.sv
   All pred_comb.* and upd_p0.* field accesses updated to
   use lp_-prefixed names. No logic changes.
3. frontend/branch_predictor/tb/tb_loop_pred.sv
   All pred_p0.* and upd_p0.* field accesses updated.
   No test logic changes.
4. planning/interfaces/loop_pred_interfaces.md
   pred_p0 and upd_p0 field label lines and inline
   references updated to lp_-prefixed names.

## Test Case Results
TC1  -- Cold miss:           PASS
TC2  -- Backward alloc:      PASS
TC3  -- Forward no-alloc:    PASS
TC4  -- Low-conf hit:        PASS
TC5  -- Confidence at max:   PASS
TC6  -- Trusted taken:       PASS
TC7  -- Trusted exit:        PASS
TC8  -- Correct exit:        PASS
TC9  -- Wrong exit:          PASS
TC10 -- Mispredicted exit:   PASS
TC11 -- Victim selection:    PASS
TC12 -- Way conflict:        PASS
TC13 -- curr_itr saturation: PASS
ALL TC1-TC13 TESTS PASSED

## Lint output
verilator --lint-only --sv -Wall -Wno-UNUSED -Wno-IMPORTSTAR
  --timing -Wno-VARHIDDEN
  rtl/bp_defines_pkg.sv rtl/bp_structs_pkg.sv rtl/loop_pred.sv
(exit 0, zero warnings)

## Assumptions made not explicit in the prompt
None. All renames were explicit in the prompt.

## Decisions made not explicit in the prompt
- lp_hit in lp_pred_t already had the prefix and was not
  renamed (confirmed by binding decision).
- pc, target, actual_taken in lp_upd_t are not renamed
  (confirmed by binding decision).
- bp_loop_meta_t not touched (confirmed by binding decision).
- curs and curs_v are not used directly in tb_loop_pred.sv
  via upd_p0 field assignments (only indirectly via packed
  struct clear upd_p0 = '0). No change needed for those
  fields in the TB.
- In loop_pred_interfaces.md, double-replacement was caught
  and corrected: the ordered replace_all operations caused
  lp_pred_is_loop and lp_pred_taken to temporarily appear
  as lp_lp_pred_is_loop / lp_lp_pred_taken in three
  locations. All fixed to the correct single-prefix form.

## RVA23 compliance risks and gaps noticed
None introduced by this rename pass.

## Deferred Work
- bp_cluster.md:411 lp_set -> lp_idx (manual, flagged in
  BP-017a deferred work, not in scope for this session).

## Other Notes
None.

## Files Modified
Not captured

:: RESULTS:END ::

