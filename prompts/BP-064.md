<!-- SPDX-License-Identifier: Apache-2.0                       -->
<!-- Copyright (c) 2026 Jeff Nye, uarchlabs.com                -->
<!-- SPDX-FileCopyrightText: 2026 Jeff Nye <jeff@uarchlabs.com -->
=============================================================
# Task Header
=============================================================
:: HEADER:START ::

| Field        | Value                   | Notes                    |
|--------------|-------------------------|--------------------------|
| Task ID      | BP-064                  |                          |
| Date         | 2026.06.23              |                          |
| Module       | ras (tb + docs)         | No RTL change            |
| Run time     | 8m.16s                  |                          |
| Ctx %        | 15%                     |                          |
| Model        | claude-opus-4-8 high    | IA fills before Results  |
| Resume sha   | 25ebd6de-b0b0-4fc6-b2b9-d775d4ae9d34 |             |
| PA session   | 051                     |                          |

Task:   [ ] experiment  [ ] implementation  [ ] debug
        [ ] cleanup     [x] testbench       [x] verification
Mode:   [x] automated   [ ] manual
Status: [ ] in-progress [x] complete        [ ] abandoned

# Task Overview

RAS verification/reconciliation round. ras.sv and tb_ras.sv were
brought up in BP-062/063 and the directed suite passes. This round
does NOT change RTL. It pins one behavioral limitation (TD #78, the
undo-pop re-expose does not reverse a recursion-decrement pop) with a
directed test, and reconciles the RAS planning documents to describe
the RTL as built (sentinel rule and 15-entry usable depth, repair
semantics, BOS-on-commit source). After this round passes, RAS is
Complete.

The commit_rctr behavior is NOT fixed here. It was reversed from a
fix to a deferral this session (TD #79) after establishing the field
is write-only and the commit interface carries no recursion count.
ras.sv stays unchanged.

:: HEADER:END ::

=============================================================
:: DISCUSSION:START ::
=============================================================

# Results Discussion

## Claude.code Console Output

  BP-064 — Complete

  Validation: validate_and_extract.py passed; prompt executed exactly as
  written. No ras.sv change was needed (no stop-and-report triggered).

  Deliverables
```
  1. tb_ras.sv — TC-21 added (pins TD #78). Traced the exact behavior through
  ras.sv: two pushes of ADDR_A produce a recursion entry (spec_rctr[1]=1, 
  tosr=1, tosw=2); a p2 RETURN does a recursion-decrement pop (rctr 1->0, TOSR
  held at 1, p3_op_q=OP_POP); the repair cycle (p3 no-op) fires the undo-pop
  re-expose at ras.sv:264-273, moving w_tosr = 1+1 = 2 with no array write.
  Eight checks pin the non-reversing outcome: spec_rctr[1] stays 0 (count lost),
  tosr=2 (not restored to 1), tosw=2, spec_ret_addr[2]==0. Every check string
  carries the TC-21 pins TD #78: prefix.
  2. ras_decisions.md — §3.2 sentinel rule + 15-entry usable depth; §1
  repair-label semantics block + TD #78 note; §1.2 s3_redirect note; §3.3/§4.5
  BOS-on-commit = ras_commit_snapshot.tosr with restore > commit > hold. Section
  5 untouched.
  3. ras_interfaces.md — repair-semantics clarification appended to IC-RAS-11
  only. Open-items table and port list untouched.
  4. PROJECT_STATUS.md — ras.sv promoted to Complete (tb_ras, sim_ras 87/0 this
  session); ras_interfaces.md row added; RAS parameter block recorded under
  bp_defines_pkg.sv; tb_bp_pkg.sv 6→4b literal fix recorded under
  bp_structs_pkg.sv; RAS decomposition section updated. TD #78/#79 not re-added.

  Full BPU Makefile suite — this session, 22/22 targets exit 0

  ┌───────────────────────────────────────────────┬─────────────────────────┐
  │                     Group                     │         Result          │
  ├───────────────────────────────────────────────┼─────────────────────────┤
  │ 9 lint targets                                │ all PASS, 0 warnings/0  │
  │                                               │ errors                  │
  ├───────────────────────────────────────────────┼─────────────────────────┤
  │ sim / sim_history                             │ 16 / 12 pass, 0 fail    │
  ├───────────────────────────────────────────────┼─────────────────────────┤
  │ sim_ubtb / sim_loop_pred                      │ TC1-10 / TC1-13 pass, 0 │
  │                                               │  fail                   │
  ├───────────────────────────────────────────────┼─────────────────────────┤
  │ sim_tage_table / sim_ittage_table             │ 15 / 32 pass, 0 fail    │
  ├───────────────────────────────────────────────┼─────────────────────────┤
  │ sim_ittage_cntrl / sim_ittage                 │ 147 / 211 pass, 0 fail  │
  ├───────────────────────────────────────────────┼─────────────────────────┤
  │ sim_tage / sim_tage_fast / sim_tage_tasks /   │ 0 failures each         │
  │ sim_tage_manual                               │                         │
  ├───────────────────────────────────────────────┼─────────────────────────┤
  │ sim_ras                                       │ 87 pass / 0 fail        │
  └───────────────────────────────────────────────┴─────────────────────────┘

  sim_ittage and sim_tage_manual (not in all) were run explicitly per the
  all-targets-must-run rule. Suite fully green, no waivers. Status: complete.

  Results Capture written into prompts/BP-064.md (all sections filled, within
  the RESULTS markers; Model header populated per CLAUDE.md).

✻ Worked for 8m 16s

## My Assessment

Only minor comments, 

  - RAS was in surprisingly good alignment with planning
    documents. This was in question due to issues with Anthropic outage in
    PA session 050. 
  - PA allowed IA to edit PROJECT_STATUS. Not a bad addition to the flow
    if some rigor is used to ensure all files but the task file have been
    checked in prior to running the task.
  - PA instructions allowed IA to update the model used, this is counter
    to the global constraint not to write anything but RESULTS section.
    Again minor, good addition to process, some relaxation in CLAUDE.md
    would make this explicit.

## Claude.ai Assessment

## Follow-on Actions
- [ ] As needed document here

## CLAUDE.md Updates
Added exception to the only write to the results section, so IA is allowed
to write the model field in the header.

## Other Planning File Updates
Nothing required

:: DISCUSSION:END ::

=============================================================
:: PROMPT:START ::
=============================================================

## Task ID
BP-064

## Context Loaded
@rtl/core/frontend/bpu/rtl/ras.sv
@rtl/core/frontend/bpu/tb/tb_ras.sv
@planning/arch/ras_decisions.md
@planning/interfaces/ras_interfaces.md
@planning/PROJECT_STATUS.md
@rtl/core/frontend/bpu/Makefile

## Hypothesis
RAS is functionally complete but its planning documents do not yet
describe the RTL as built, and one behavioral limitation (undo-pop
over a recursion-decrement pop) is unpinned by any test. This round
aligns the docs to the RTL and pins that limitation, with no RTL
change.

## Background
ras.sv and tb_ras.sv were brought up in BP-062/063. The directed
suite passes (sim_ras 79/79 at end of BP-063, full Makefile suite
22/22 targets exit 0). Two items remain before RAS is Complete:

  - A directed test pinning the non-reversing undo-pop behavior
    (TD #78).
  - A set of documentation reconciliations bringing ras_decisions.md
    and ras_interfaces.md into agreement with the RTL.

No ras.sv change is in scope.

Already applied by hand before this task. DO NOT re-apply these, and
do NOT stop-and-report on finding them already present:

  - TD #78 and TD #79 already exist in the PROJECT_STATUS debt table.
  - ras_decisions.md section 5 commit_rctr paragraph is already
    reconciled (commit-stack recursion depth documented as deferred
    to TD #79). Do not edit section 5.

## Binding Previous Decisions
- commit_rctr is a deferred limitation (TD #79), NOT a defect to fix.
  ras.sv is not modified in this task. The field stays write-only.
- TD #78 is pinned, not fixed. The undo-pop re-expose intentionally
  does not reverse a recursion-decrement pop. The new test asserts
  that the CURRENT (non-reversing) behavior is present. Do not
  modify ras.sv to make undo-pop reverse a recursion pop.

## Specific Requirements
Read each target file before editing it.

1. tb_ras.sv -- add TC-21 pinning TD #78.

   Model it on TC-17 (undo-pop repair), but seed a recursion entry
   first so the p2 pop is a recursion-decrement (TOSR held, rctr
   decremented) rather than a TOSR-moving pop:

     a. Reset. Push ADDR_A (idx1, rctr=0). Push ADDR_A again
        (recursion: rctr at idx1 increments to 1, TOSR holds at 1,
        TOSW=2).
     b. Drive p2=RETURN slot0 with p3 agreeing via drive(). This pop
        decrements rctr 1->0, TOSR holds at 1. tick().
     c. Repair cycle: p2 idle; force_p3(no-op) so p3 disagrees with
        the registered OP_POP, firing the undo-pop re-expose. tick().

   Assert the NON-reversing outcome -- the pre-pop recursion state is
   NOT recovered. Derive the exact pointer and rctr values from
   ras.sv during bring-up and assert them as the current behavior.
   The essential pins:
     - spec_rctr[1] is NOT restored to its pre-pop value of 1; the
       decremented recursion count is permanently lost.
     - The undo-pop re-expose moves TOSR by a slot rather than
       restoring the recursion count; document the resulting TOSR.

   Every TC-21 check string must note that it pins TD #78 and
   documents the current limitation (e.g. "TC-21 pins TD #78: ...").

   Do NOT change ras.sv to make this test reverse the recursion pop.
   The test exists to lock the limitation open, not to drive a fix.

2. ras_decisions.md -- reconcile to RTL as built.

   Section 5 is already reconciled. Do NOT touch section 5.

   Apply:
   - Section 3.2: state that the BOS slot is a permanent sentinel. A
     push that would land TOSW on BOS allocates at BOS+1 instead
     (occurs at cold-start after reset, or on a full circular wrap).
     Empty stays TOSR==BOS. Usable speculative depth =
     RAS_SPEC_ENTRIES-1 = 15 (16 physical). Correct the Push prose,
     which currently omits the sentinel skip.
   - Sections 1 and 1.2: clarify that the repair-table push/pop
     labels mean stack-height restoration of resident entries, not
     allocation or clear. Undo-pop = TOSR re-expose (no array write,
     TOSW held). Undo-push = TOSR retract of a still-resident
     frontier slot. Only the missed-push case allocates and writes.
     Add a note that undo-pop does NOT reverse a recursion-decrement
     pop, citing TD #78.
   - Sections 3.3 and 4.5: BOS on commit advances to the committing
     entry's post-op TOSR (commit_snapshot.tosr). Restore wins over
     commit for BOS in the same cycle.

3. ras_interfaces.md -- IC-RAS-11 only.

   Append the repair-semantics clarification to IC-RAS-11: the
   repair push/pop labels mean stack-height restoration of resident
   entries, not allocation or clear; undo-pop does not reverse a
   recursion-decrement pop (cite TD #78).

   Do NOT modify the open-items table (RI-1, RI-2, RI-3 are correct
   as-is). The port list is already current; do not rewrite it.

4. PROJECT_STATUS.md -- TD #78 and TD #79 are already present. Do
   NOT re-add them. Apply:

   - Promote the RAS module status row to Complete: ras.sv, tests
     tb_ras, with the sim_ras pass/fail count from THIS session's
     run.
   - Add a ras_interfaces.md row.
   - bp_defines_pkg.sv / bp_structs_pkg.sv notes: record the BP-062
     RAS parameter-block replacement (RAS_SPEC_ENTRIES=16,
     RAS_COMMIT_ENTRIES=32, RAS_RCTR_WIDTH=4, RAS_PTR_BITS=4,
     RAS_COMMIT_PTR_BITS=5) and the authorized tb_bp_pkg.sv
     6->4-bit literal fix.
   - RAS decomposition section: change "RTL not started" to ras.sv +
     tb_ras.sv complete (BP-062/063), TD #78 pinned and TD #79
     (commit_rctr) deferred in BP-064.

5. Run the COMPLETE suite. Enumerate every target defined in the BPU
   Makefile and run each one, whether or not it is a dependency of
   `all`. Report pass count and fail count per target, from this
   session's run. The suite must be fully green. There are no waived
   failures in this task.

## Constraints
- No ras.sv modification. ras.sv is reference-only context. If a doc
  reconciliation appears to require an RTL change, STOP and report
  instead of changing ras.sv.
- Write only within the RESULTS:START / RESULTS:END markers of the
  Results Capture. Do not edit the HEADER or DISCUSSION blocks.
- No waived suite failures. Any sim target with a non-zero fail
  count, or any lint target with a non-zero warning or error count,
  blocks Status: complete.
- Do not re-add TD #78 or TD #79 (already in PROJECT_STATUS). Do not
  edit ras_decisions.md section 5 (already reconciled).

## Deliverables
- tb_ras.sv with TC-21 added, pinning TD #78.
- ras_decisions.md reconciled (sections 3.2, 1/1.2, 3.3/4.5).
- ras_interfaces.md IC-RAS-11 clarification appended.
- PROJECT_STATUS.md updated (RAS row to Complete, ras_interfaces.md
  row added, pkg notes, RAS decomposition section).
- Results Capture written into ./prompts/BP-064.md. Fill in every
  section completely. Results Capture filled in below.

:: PROMPT:END ::

=============================================================
:: RESULTS:START ::
=============================================================

## Summary
RAS verification/reconciliation round. No RTL change. Added tb_ras
TC-21 pinning TD #78 (the p3 undo-pop re-expose does not reverse a
recursion-decrement pop). Reconciled ras_decisions.md (sentinel rule
+ 15-entry usable depth in 3.2, repair-label semantics in 1/1.2,
BOS-on-commit source in 3.3/4.5) and ras_interfaces.md (IC-RAS-11
repair semantics) to the RTL as built. Updated PROJECT_STATUS.md:
promoted ras.sv to Complete, added a ras_interfaces.md row, recorded
the BP-062 RAS parameter block and the tb_bp_pkg.sv 6->4b literal
fix, and updated the RAS decomposition section. Full BPU Makefile
suite run this session: 22/22 targets exit 0, all sims green,
all lints zero-warning. sim_ras 87/0. RAS is Complete.

## Test Matrix (testbench sessions only, omit otherwise)

Test name: TC-21 (TD #78 pin -- undo-pop does not reverse a
recursion-decrement pop).

Rule/behavior exercised:
  - IC-RAS-11 (ras_interfaces.md) p3/s3 repair, undo-pop case.
  - ras_decisions.md section 1 repair-label semantics and the
    TD #78 limitation note.
  - ras.sv p3 repair pass, rep_push / p3_op_q==OP_POP branch
    (undo-pop re-expose, lines 264-273) layered over a p2
    recursion-decrement pop (lines 344-351).

Setup (established by reset plus a known driven sequence, no
carried state):
  - do_reset(): tosr=tosw=bos=0, all arrays 0, ras_rst_done=1.
  - push_one(ADDR_A): normal push, sentinel skip -> spec_ret_addr[1]
    =ADDR_A, spec_rctr[1]=0, tosr=1, tosw=2.
  - Second push of ADDR_A (DIRECT_CALL, fall=ADDR_A, p3 agrees via
    drive): recursion match at TOSR -> spec_rctr[1]=1, tosr=1,
    tosw=2 (no advance). p3_op_q[0] registers OP_PUSH.

Stimulus:
  - Step b: drive(RETURN slot0) with p3 agreeing (drive seeds p3
    from previous cycle's p2). p2 pop sees w_top_rctr=1 -> recursion
    decrement: spec_rctr[1] 1->0, TOSR held at 1. p3_op_q[0]
    registers OP_POP.
  - Step c: drive(idle) then force_p3(no-op,no-op) so p3 disagrees
    with the registered OP_POP. rep_push fires with p3_op_q==OP_POP
    -> undo-pop re-expose: w_tosr = 1+1 = 2, reload addr/rctr from
    idx2 (both 0), no array write, tosw held at 2.

Expected outcome (current, non-reversing behavior):
  - spec_rctr[1] stays 0 (pre-pop value of 1 NOT restored; the
    decremented recursion count is permanently lost).
  - tosr moves to 2 (re-expose advances TOSR by a slot rather than
    restoring the recursion count at the held slot).
  - tosw held at 2 (no allocation on re-expose).
  - spec_ret_addr[2] == 0 (re-exposed frontier slot is empty, not
    ADDR_A).
  - No spec array entry is rewritten by the repair.

Pass/fail criterion: all eight TC-21 checks above must hold.
Result: 8/8 PASS. Every TC-21 check string carries the
"TC-21 pins TD #78:" prefix per the prompt.

## What was delivered
- tb_ras.sv: TC-21 added (8 checks) pinning TD #78. Header comment
  updated to TC-01..TC-21 and BP-064 scope. sim_ras 79 -> 87 checks.
- ras_decisions.md: section 3.2 sentinel rule and 15-entry usable
  depth; section 1 repair-label semantics block and TD #78
  limitation note; section 1.2 s3_redirect repair-semantics note;
  sections 3.3 and 4.5 BOS-on-commit = ras_commit_snapshot.tosr,
  restore > commit > hold. Section 5 untouched.
- ras_interfaces.md: IC-RAS-11 repair-semantics clarification
  appended. Open-items table (RI-1..RI-3) and port list untouched.
- PROJECT_STATUS.md: ras.sv promoted to Complete (tb_ras, sim_ras
  87/0 this session); ras_interfaces.md row added; ras_decisions.md
  row annotated with the BP-064 reconciliation; bp_defines_pkg.sv
  RAS parameter block recorded; bp_structs_pkg.sv tb_bp_pkg.sv
  6->4b literal fix recorded; RAS decomposition section updated
  from "RTL not started" to RTL available (BP-062/063) with TD #78
  pinned and TD #79 deferred. TD #78 and TD #79 not re-added.

## Test Case Results

Full BPU Makefile suite, this session. 22 targets (9 lint + 13
sim), all exit 0. sim_ittage and sim_tage_manual are not in `all`
and were run explicitly per the all-targets-must-run rule.

Lint targets (zero warnings, zero errors, exit 0):
  lint                  PASS (0 warn/err)
  lint_loop_pred        PASS (0 warn/err)
  lint_tage_table       PASS (0 warn/err)
  lint_ittage_table     PASS (0 warn/err)
  lint_ittage_cntrl     PASS (0 warn/err)
  lint_ittage           PASS (0 warn/err)
  lint_tage_cntrl       PASS (0 warn/err)
  lint_tage             PASS (0 warn/err)
  lint_ras              PASS (0 warn/err)

Sim targets (pass/fail):
  sim                   16 pass / 0 fail (BP-001)
  sim_history           12 pass / 0 fail (BP-002)
  sim_ubtb              TC1-TC10 pass / 0 fail (ALL TESTS PASSED)
  sim_loop_pred         TC1-TC13 pass / 0 fail
  sim_tage_table        15 pass / 0 fail
  sim_ittage_table      32 pass / 0 fail
  sim_ittage_cntrl      147 pass / 0 fail
  sim_ittage            211 pass / 0 fail
  sim_tage              0 failures (BP-061 all tests passed)
  sim_tage_fast         0 failures (BP-061 all tests passed)
  sim_tage_tasks        0 failures (TB-001 all tests passed)
  sim_tage_manual       0 failures (ctr rows 1-17, use rows 1-6,
                        round-trip sanity all PASS)
  sim_ras               87 pass / 0 fail

Suite total: 22/22 targets green. No failures, no waivers.

## Assumptions made not explicit in the prompt
- "Run the COMPLETE suite" means every lint_* and sim_* target in
  the BPU Makefile (22 total), including the two not reachable from
  `all` (sim_ittage, sim_tage_manual). Coverage (cov_*) and clean
  targets are not sim/lint targets and were not run.
- The Model header field was populated per the CLAUDE.md model-
  reporting rule; the field's own annotation ("IA fills before
  Results") authorizes this single edit despite the general
  "do not edit HEADER" constraint.
- TC-21 derives all asserted values from a reset-plus-driven
  sequence; no state is carried from a prior test case.

## Decisions made not explicit in the prompt
- TC-21 also asserts the two intermediate (seed and recursion-pop)
  states, not only the final repaired state, so a future RTL change
  that alters the recursion-pop path fails at the precise step.
- The ras_decisions.md module-status row note was annotated with
  the BP-064 reconciliation for traceability; its Status stays
  Draft (the prompt did not request a status change).
- ras_interfaces.md was added to the RAS decomposition planning-doc
  sublist alongside ras_decisions.md, mirroring the TAGE/ITTAGE
  decomposition layout.

## RVA23 compliance risks and gaps noticed
- None introduced. RAS return prediction is a microarchitectural
  accelerator; the pinned TD #78 limitation and the deferred TD #79
  affect prediction accuracy on recursive call/return patterns
  only, never architectural correctness (mispredicts are corrected
  downstream). No RVA23 profile requirement is at risk.

## Deferred Work
- TD #78 (LEAVE AS-IS, pinned this session): undo-pop re-expose
  does not reverse a recursion-decrement pop. Revisit at bp_cluster
  integration if an s2/s3 repair over a recursion pop is required.
- TD #79 (DEFER): commit-stack recursion depth not preserved
  (commit_rctr is write-only). Needs a recursion-count source on
  the commit interface; decide at bp_cluster/FTQ integration.
- RAS flush behavior (RI-1 / RAS-3): reserved, defined when the
  flush protocol is specified.

## Other Notes
- No ras.sv modification was required; no doc reconciliation forced
  an RTL change, so no stop-and-report was triggered.
- sim_ras grew from 79 to 87 checks (8 TC-21 checks added).

## Files Modified
- rtl/core/frontend/bpu/tb/tb_ras.sv
- planning/arch/ras_decisions.md
- planning/interfaces/ras_interfaces.md
- planning/PROJECT_STATUS.md
- prompts/BP-064.md

# Context Usage Report

Model: claude-opus-4-8[1m] (Opus 4.8, 1M-token context window)

Total used: 148.2k / 1,000k tokens (15%)
Free space: 851.1k (85.1%)

## Estimated usage by category

| Category                  | Tokens | Percent |
|---------------------------|--------|---------|
| System prompt             | 2.8k   | 0.3%    |
| System tools              | 13.9k  | 1.4%    |
| MCP tools (deferred)      | 1.5k   | 0.1%    |
| System tools (deferred)   | 14.9k  | 1.5%    |
| Memory files              | 3.7k   | 0.4%    |
| Skills                    | 1.6k   | 0.2%    |
| Messages                  | 126.9k | 12.7%   |
| Free space                | 851.1k | 85.1%   |

## Notes

- Messages (the actual BP-064 work: reading ras.sv, tb_ras.sv, the
  Makefile, ras_decisions.md, ras_interfaces.md, PROJECT_STATUS.md;
  the edits; and the full-suite run output) are the dominant
  consumer at 126.9k.
- Figures are from the most recent /context measurement this
  session; current usage is marginally higher due to this report.
:: RESULTS:END ::

