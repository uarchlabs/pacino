=============================================================
# Task Header
=============================================================
:: HEADER:START ::
| Field        | Value                   | Notes                    |
|--------------|-------------------------|--------------------------|
| Task ID      | BP-022b                 |                          |
| Date         | 2026.04.10              |                          |
| Module       | bp_defines_pkg.sv       |                          |
|              | tb_tage_table.sv        |                          |
|              | tb_bp_pkg.sv            |                          |
| Run time     | 7m.7s                   |                          |
| Ctx %        | 46%                     |                          |
| Model        | Sonnet 4.6 medium       |                          |
| Resume sha   | 98163a9d-db70-4670-8996-46f7e0ff8a40 |             |
Task:   [ ] experiment  [ ] implementation  [x] cleanup  [ ] debug  [ ] testbench
Mode:   [x] automated   [ ] manual
Status: [ ] in-progress [x] complete        [ ] abandoned
# Overview of task
Remove eight backward-compat aliases added to
bp_defines_pkg.sv by BP-022a and remove SC_NUM_MAIN_TBLS.
Update the two out-of-context testbenches that required
those aliases to use canonical parameter names directly.
No RTL modules are modified. No logic changes.
:: HEADER:END ::
=============================================================
# Paste c.code console output and c.ai discussion
=============================================================
:: DISCUSSION:START ::
# Results Discussion
## Claude.code Console Output

Blocker found and resolved: SC_NUM_MAIN_TBLS had a live consumer in           
  bp_structs_pkg.sv:113 (packed array dimension in bp_sc_meta_t). Scope was
  expanded per your authorization (option 1). The dimension was replaced with   
  the literal [3:0] and the comment updated.   
                                                                                
  All changes applied:                                                          
  
  ┌───────────────────┬────────────────────────────────────────────────┐        
  │       File        │                     Change                     │
  ├───────────────────┼────────────────────────────────────────────────┤
  │ bp_defines_pkg.sv │ Removed 8 aliases + SC_NUM_MAIN_TBLS           │
  ├───────────────────┼────────────────────────────────────────────────┤
  │ bp_structs_pkg.sv │ [SC_NUM_MAIN_TBLS-1:0] → [3:0] in struct       │        
  ├───────────────────┼────────────────────────────────────────────────┤        
  │ tb_tage_table.sv  │ 8 symbol substitutions (6 names + 2 casts)     │        
  ├───────────────────┼────────────────────────────────────────────────┤        
  │ tb_bp_pkg.sv      │ SC_TBL_INDEX_BITS → SC_MAX_IDX_WIDTH (3 sites) │
  └───────────────────┴────────────────────────────────────────────────┘        
                                               
  All 12 targets green:                                                         
  - lint / lint_loop_pred / lint_tage_table / lint_tage_cntrl / lint_tage — 0
  warnings                                                                      
  - sim 16/16, sim_history 12/12, sim_ubtb TC1-TC10, sim_loop_pred TC1-TC13,
  sim_tage_table 12/12, sim_tage 46/46, sim_tage_fast 46/46

## My Assessment

Pending

## Claude.ai Assessment

Clean pass. All 12 targets green, no warnings, all test counts match. Two deferred items to note:

SC_TBL_INDEX_BITS still in bp_defines_pkg.sv with no active consumers — candidate for removal in a future housekeeping pass.
Commented-out alias block (lines 165-170) still present as dead code — same.

## Follow-on Actions
None - session handoff is next
## CLAUDE.md Updates
None
## Other Planning File Updates
None
:: DISCUSSION:END ::
=============================================================
# Claude.code Prompt
=============================================================
:: PROMPT:START ::
## Task ID
BP-022b

## Context Loaded
@frontend/branch_predictor/rtl/bp_defines_pkg.sv
@frontend/branch_predictor/tb/tb_tage_table.sv
@frontend/branch_predictor/tb/tb_bp_pkg.sv

## Hypothesis
N/A -- cleanup pass only.

## Background
BP-022a added eight backward-compat aliases to
bp_defines_pkg.sv and retained SC_NUM_MAIN_TBLS because
two testbenches were outside the context loaded list.
Those testbenches are now in context. The aliases and
SC_NUM_MAIN_TBLS can be removed once the testbenches
are updated to use canonical names directly.

## Binding Previous Decisions
  - TAGE_TBL_* vectors are the authoritative per-table
    parameter source (debt #24, closed BP-019a).
  - TAGE_MAX_* and SC_MAX_* are the canonical field-width
    parameters (BP-022, BP-022a).
  - Package import order: bp_defines_pkg then
    bp_structs_pkg, file scope before module declaration.

## Specific Requirements

### Change 1: Remove aliases from bp_defines_pkg.sv

Search for and remove the following nine items.
Before removing each one, confirm no consumer exists
outside tb_tage_table.sv and tb_bp_pkg.sv. If any
other consumer is found, stop and report.

Eight backward-compat aliases:
  MAX_IDX_WIDTH   (alias for TAGE_MAX_IDX_WIDTH)
  MAX_TAG_WIDTH   (alias for TAGE_MAX_TAG_WIDTH)
  MAX_EPC_WIDTH   (alias for TAGE_MAX_EPC_WIDTH)
  MAX_USE_WIDTH   (alias for TAGE_MAX_USE_WIDTH)
  MAX_CTR_WIDTH   (alias for TAGE_MAX_CTR_WIDTH)
  MAX_VAL_WIDTH   (alias for TAGE_MAX_VAL_WIDTH)
  TAGE_T1_FH      (alias for TAGE_MAX_FH)
  TAGE_T1_FH1     (alias for TAGE_MAX_FH1)

One retained localparam:
  SC_NUM_MAIN_TBLS

### Change 2: Update tb_tage_table.sv

Apply the following substitutions. Change symbol names
only. Do not modify any logic, test cases, comments,
or structure.

  MAX_EPC_WIDTH  -> TAGE_MAX_EPC_WIDTH
  MAX_USE_WIDTH  -> TAGE_MAX_USE_WIDTH
  MAX_CTR_WIDTH  -> TAGE_MAX_CTR_WIDTH
  MAX_VAL_WIDTH  -> TAGE_MAX_VAL_WIDTH
  MAX_IDX_WIDTH  -> TAGE_MAX_IDX_WIDTH
  MAX_TAG_WIDTH  -> TAGE_MAX_TAG_WIDTH
  TAGE_T1_FH'    -> TAGE_MAX_FH'
  TAGE_T1_FH1'   -> TAGE_MAX_FH1'

### Change 3: Update tb_bp_pkg.sv

Apply the following substitution. Change symbol name
only. Do not modify any logic, test cases, comments,
or structure.

  SC_TBL_INDEX_BITS -> SC_MAX_IDX_WIDTH

## Constraints
  - Do not modify any file not listed in Context Loaded.
  - Do not modify any parameter, signal, or logic not
    named in this prompt.
  - Line width 80 columns maximum.

## Deliverables
  1. frontend/branch_predictor/rtl/bp_defines_pkg.sv
  2. frontend/branch_predictor/tb/tb_tage_table.sv
  3. frontend/branch_predictor/tb/tb_bp_pkg.sv

Run in order after all edits:
  make lint
  make lint_loop_pred
  make lint_tage_table
  make lint_tage_cntrl
  make lint_tage
  make sim
  make sim_history
  make sim_ubtb
  make sim_loop_pred
  make sim_tage_table
  make sim_tage
  make sim_tage_fast

All targets must exit 0 with 0 warnings.
sim must report 16/16 checks PASS.
sim_history must report 12/12 PASS.
sim_ubtb must report TC1-TC10 PASS.
sim_loop_pred must report TC1-TC13 PASS.
sim_tage_table must report 12/12 PASS.
sim_tage must report 46/46 PASS.
sim_tage_fast must report 46/46 PASS.

Write Results Capture into prompts/BP-022b.md.
Fill in every section. Results Capture must be
ASCII only.
:: PROMPT:END ::
=============================================================
# Results Capture
=============================================================
:: RESULTS:START ::
## Summary
PASS. All aliases removed. SC_NUM_MAIN_TBLS removed after
expanding scope to include bp_structs_pkg.sv (user-authorized).
All 12 make targets exit 0 with 0 warnings. All sim checks pass.

## What was delivered
1. frontend/branch_predictor/rtl/bp_defines_pkg.sv
   - Removed 8 backward-compat alias localparams:
     MAX_IDX_WIDTH, MAX_TAG_WIDTH, MAX_EPC_WIDTH, MAX_USE_WIDTH,
     MAX_CTR_WIDTH, MAX_VAL_WIDTH, TAGE_T1_FH, TAGE_T1_FH1.
   - Removed SC_NUM_MAIN_TBLS localparam.
   - SC_TBL_INDEX_BITS definition retained (not in removal list).
2. frontend/branch_predictor/rtl/bp_structs_pkg.sv  (scope expanded)
   - Replaced SC_NUM_MAIN_TBLS-1 with 3 in bp_sc_meta_t struct
     packed array dimension: [SC_NUM_MAIN_TBLS-1:0] -> [3:0].
   - Updated comment above the struct to remove reference to
     SC_NUM_MAIN_TBLS and clarify 4 tables / SC_MAX_IDX_WIDTH each.
3. frontend/branch_predictor/tb/tb_tage_table.sv
   - 8 symbol substitutions applied (replace_all):
     MAX_EPC_WIDTH  -> TAGE_MAX_EPC_WIDTH
     MAX_USE_WIDTH  -> TAGE_MAX_USE_WIDTH
     MAX_CTR_WIDTH  -> TAGE_MAX_CTR_WIDTH
     MAX_VAL_WIDTH  -> TAGE_MAX_VAL_WIDTH
     MAX_IDX_WIDTH  -> TAGE_MAX_IDX_WIDTH  (4 sites)
     MAX_TAG_WIDTH  -> TAGE_MAX_TAG_WIDTH  (2 sites)
     TAGE_T1_FH1'   -> TAGE_MAX_FH1'      (cast at TC12)
     TAGE_T1_FH'    -> TAGE_MAX_FH'       (cast at TC11)
4. frontend/branch_predictor/tb/tb_bp_pkg.sv
   - SC_TBL_INDEX_BITS -> SC_MAX_IDX_WIDTH (comment + 2 code sites).

## Test Case Results
make lint           : exit 0, 0 warnings
make lint_loop_pred : exit 0, 0 warnings
make lint_tage_table: exit 0, 0 warnings
make lint_tage_cntrl: exit 0, 0 warnings
make lint_tage      : exit 0, 0 warnings
make sim            : exit 0, 16/16 PASS
make sim_history    : exit 0, 12/12 PASS
make sim_ubtb       : exit 0, TC1-TC10 PASS
make sim_loop_pred  : exit 0, TC1-TC13 PASS
make sim_tage_table : exit 0, 12/12 PASS (TESTBENCH PASSED)
make sim_tage       : exit 0, BP-016 all 46 tests passed
make sim_tage_fast  : exit 0, BP-016 all 46 tests passed

## Assumptions made not explicit in the prompt
A1. SC_NUM_MAIN_TBLS = 4 is a fixed architectural constant
    (ST0-ST3). Replaced with the literal [3:0] packed array
    bound in bp_structs_pkg.sv. The comment was updated to make
    the 4-table count explicit without referencing the removed
    parameter.

## Decisions made not explicit in the prompt
D1. bp_structs_pkg.sv was not in the original Context Loaded
    list. SC_NUM_MAIN_TBLS was found to have a live consumer
    there (struct field dimension). Stopped and reported per
    prompt instruction. User authorized option 1: expand scope
    to include bp_structs_pkg.sv and inline the literal.
D2. Used [3:0] (literal 3 as upper bound) rather than
    SC_NUM_ALL_TBLS-2 in the struct. SC_NUM_ALL_TBLS-2 would
    be arithmetically correct but obscures intent; [3:0] is
    self-evident for a 4-entry array and matches the comment.
D3. Commented-out dead code in bp_defines_pkg.sv (lines 165-170,
    the old alias definitions already in // comments) was left
    in place. The prompt constraint says not to modify content
    outside the named items; those lines are already inert.

## RVA23 compliance risks and gaps noticed
None. This is a pure symbol-name cleanup. No RTL logic, port
widths, or parameter values were changed.

## Deferred Work
- SC_TBL_INDEX_BITS definition remains in bp_defines_pkg.sv.
  It is not referenced by any currently active consumer after
  the tb_bp_pkg.sv update. Removal can be deferred to the next
  SC cleanup pass if confirmed no new consumers are added.
- Commented-out old alias definitions (lines 165-170 of
  bp_defines_pkg.sv) remain as dead code. May be cleaned up
  in a future housekeeping pass.

## Other Notes
- TAGE_T1_FH1 was renamed before TAGE_T1_FH to avoid a
  prefix match causing TAGE_T1_FH1 to be partially rewritten
  during the TAGE_T1_FH pass. Order was: FH1 first, FH second.
- bp_structs_pkg.sv change is the only structural edit in this
  session that touches an RTL package (not a testbench). Lint
  and all sim targets confirm the struct packing is unchanged.

## Files Modified
Not captured

:: RESULTS:END ::
