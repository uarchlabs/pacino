<!-- SPDX-License-Identifier: Apache-2.0                        -->
<!-- Copyright (c) 2026 Jeff Nye, uarchlabs.com                 -->
<!-- SPDX-FileCopyrightText: 2026 Jeff Nye <jeff@uarchlabs.com> -->
=============================================================
# Task Header
=============================================================
:: HEADER:START ::

| Field        | Value                   | Notes                    |
|--------------|-------------------------|--------------------------|
| Task ID      | BP-054a                 |                          |
| Date         | 2026.06.09              |                          |
| Module       | ittage                  |                          |
| Run time     | 3m.32s                  |                          |
| Ctx %        | 72%                     |                          |
| Model        | claude-sonnet-4-6 normal |                         |
| Resume sha   | b0ffc432-182f-482b-8f6e-d76461bc1b3c |             |
| PA session   | 048                     |                          |

Task:   [ ] experiment  [ ] implementation  [ ] debug
        [x] cleanup     [ ] testbench       [x] verification
Mode:   [x] automated   [ ] manual
Status: [ ] in-progress [x] complete        [ ] abandoned

# Task Overview

Cleanup after BP-054. The two prediction-path document gaps
BP-054 reported have been corrected in ittage_cntrl_decisions.md:
pred_strong is now "provider CTR != 0", and the final-target
section now states the consumer selects ittage_prm_tgt or
ittage_alt_tgt using ittage_using_primary (no single
ittage_pred_tgt field). Treat the documents as correct and
consistent. Re-check the pred_strong and final-target rules
against the corrected document, and confirm no ittage_pred_tgt
field or reference remains in the struct package, RTL, or
testbench.

:: HEADER:END ::

=============================================================
:: DISCUSSION:START ::
=============================================================

# Results Discussion

## Claude.code Console Output

● BP-054a is complete. All findings:

  pred_strong — CONFORMS. Document row (Decoration flags,
  ittage_cntrl_decisions.md): "provider ctr was != 0". RTL at
  ittage_cntrl.sv:554 uses final_ctr != 0 where final_ctr is the UAON-muxed
  selected-provider CTR. TC-PRED-07's four sub-cases (using_prm={1,1,0,0},
  CTR={2,0,3,0}) each check exactly that rule. No conflict.

  Final target — CONFORMS. Document section "Final target (p1, combinational)"
  states two separate fields plus a selector. bp_structs_pkg.sv carries
  ittage_prm_tgt and ittage_alt_tgt as separate struct fields with
  ittage_using_primary. RTL stores them separately. TC-PRED-08 reads both fields
  and the selector in both cases. No conflict.

  ittage_pred_tgt search — zero occurrences in all three files.

  No code changes were needed. All 22 Makefile sim and lint targets pass
  (164/164 for sim_ittage, 147/147 for sim_tage_tasks, all others clean).
  Results Capture written to prompts/BP-054a.md, status set to complete.

✻ Worked for 3m 32s

※ recap: BP-054a verified that the corrected ittage_cntrl_decisions.md is fully 
  consistent with the RTL and testbench, with no ittage_pred_tgt residue found 
  and all 22 Makefile targets passing clean. No follow-on action is required. 
  (disable recaps in /config)


## My Assessment

Everything seems fine. 

Worked for 3m.7s and yet used 71% context. 

Total lines in the file do not seem to account for 71% context.
Is it possible running all targets produces a lot output ?

planning/arch/ittage_cntrl_decisions.md        254 lines
rtl/core/frontend/bpu/rtl/ittage_cntrl.sv      856 lines
rtl/core/frontend/bpu/rtl/bp_structs_pkg.sv    455 lines
rtl/core/frontend/bpu/rtl/bp_defines_pkg.sv    265 lines
rtl/core/frontend/bpu/tb/tb_ittage.sv         2145 lines
rtl/core/frontend/bpu/Makefile                 568 lines

ME: did compaction occur ?

IA: No. The autocompact buffer shows 33k reserved but the conversation summary
  indicator would appear at the top of context if compaction had fired. The
  usage hit 72% this session from reading large files (tb_ittage.sv at 2145
  lines, the full Makefile, all RTL) plus the sim output — but it stayed under
  the compaction threshold.

NOTE PA assessment: "That's the real cost of the all-targets rule: every 
 task now carries 22 targets' worth of console output regardless of task size.

This run all targets was introduced to mitigate introduction of 
upstream errors due to downstream work. I will implement a phased
approach to this so that early tasks are not burdened with this context
load.


## Claude.ai Assessment

Clean. Both rules conform to the corrected doc, TC-PRED-07/08 already check the right rules, zero ittage_pred_tgt occurrences, all 22 targets pass. No code change needed. #65 cleanup done — the prediction path is fully aligned with the corrected document.

On your context question: yes, the 72% is mostly the run, not the files. The six files are ~4,600 lines ≈ 35-40k tokens loaded once. The rest is the 22-target run output accumulating in the message history — each sim/lint invocation's stdout stays in context, and "all targets" is 22 of them. A 3-minute task burning 124k message tokens is the build output, not reading. Compaction didn't fire; it just ran hot.

That's the real cost of the all-targets rule: every task now carries 22 targets' worth of console output regardless of task size. For a tiny re-check task that's a poor ratio, but it's the price of the no-regression guarantee and you've decided that's worth it. If it ever pushes a task into compaction mid-run, the lever is piping sim output to a file and having the IA report only the pass/fail summary line per target, not the full log — but don't change that unless it actually starts forcing compaction.

ITTAGE directed paths all closed now: #56, #57, #59, #61, #63, #65. Only #72 (round-trip capstone) remains, and its gate is exactly those six. That's the last ITTAGE task.

## Follow-on Actions
Nothing required

## CLAUDE.md Updates
Nothing required

## Other Planning File Updates
Nothing required

:: DISCUSSION:END ::

=============================================================
:: PROMPT:START ::
=============================================================

## Task ID
BP-054a

## Context Loaded
@planning/arch/ittage_cntrl_decisions.md
@rtl/core/frontend/bpu/rtl/ittage_cntrl.sv
@rtl/core/frontend/bpu/rtl/bp_structs_pkg.sv
@rtl/core/frontend/bpu/rtl/bp_defines_pkg.sv
@rtl/core/frontend/bpu/tb/tb_ittage.sv
@rtl/core/frontend/bpu/Makefile

## Hypothesis

After the BP-054 document corrections, the prediction-path RTL
and the BP-054 tests are consistent with the corrected
ittage_cntrl_decisions.md, and no single ittage_pred_tgt field
or reference remains anywhere (struct, RTL, testbench). BP-054
verified the RTL against the document text as it stood before
correction; this task confirms the corrected text still matches.

## Background

BP-054 reported two document gaps and did not change RTL:
- pred_strong: the decoration-flags row read "provider CTR != 3
  and != 4" (a TAGE carryover) while the UAON section and the
  RTL used CTR != 0. The document has been corrected to
  "provider CTR != 0".
- final target: the document referenced a single ittage_pred_tgt
  field. The RTL and struct carry separate ittage_prm_tgt and
  ittage_alt_tgt, selected by the consumer via
  ittage_using_primary. The document has been rewritten to state
  this. The intent is to avoid storing a third target field.

The documents are now the correct reference. This task does not
edit them.

## Binding Previous Decisions

- ittage_cntrl_decisions.md is correct and consistent. It is the
  reference for the RTL and the testbench (BP-048). Do not edit
  the document.
- pred_strong is "selected provider CTR != 0" (null is 3'b000).
- The final target is not a stored field. The consumer selects
  ittage_prm_tgt or ittage_alt_tgt using ittage_using_primary.
- No RTL change is expected: BP-054 found the RTL already
  conformant. If the corrected document now disagrees with the
  RTL, that is a real conflict -- report it and stop.

## Specific Requirements

Read before write.

1. Re-check the pred_strong rule against the corrected
   decoration-flags row in ittage_cntrl_decisions.md. Confirm
   the RTL drives ittage_pred_strong from the selected provider
   CTR != 0, and that the BP-054 pred_strong test (the four
   sub-cases covering using_primary == 1 and == 0, selected
   provider null and not-null) checks exactly that rule. State
   conform or conflict, cite the document row.

2. Re-check the final-target rule against the corrected
   final-target section. Confirm the meta carries ittage_prm_tgt
   and ittage_alt_tgt as separate fields, the consumer selects
   between them by ittage_using_primary, and the BP-054 target
   test reads both fields and checks the selector. State conform
   or conflict, cite the document section.

3. Search the struct package (bp_structs_pkg.sv), ittage_cntrl.sv,
   and tb_ittage.sv for any remaining ittage_pred_tgt field
   declaration or reference. Report each occurrence with file and
   line, or report none found. The intent is that no single
   stored predicted-target field exists.

4. If steps 1-3 find the RTL or tests already conform and no
   ittage_pred_tgt remains, make no code change. If a test cites
   the old pred_strong rule text or the old single-field target
   model, correct the test to the current document (testbench
   change only). If the RTL conflicts with the corrected
   document, report and stop (Status in-progress) -- do not
   change RTL in a cleanup task.

5. Run every sim and lint target in the ittage Makefile, whether
   or not it is a dependency of `all`. Report pass count and
   fail count per target from this session.

## Constraints

- Documentation-alignment and re-check only. Do not change
  prediction-path RTL. Do not edit any document. Do not change
  CTR, USE, TGT, UAON, allocation, or aging logic.
- If a residual ittage_pred_tgt field is found in the struct or
  RTL, report it; do not remove it in this task (removing a
  struct field is an RTL change with wider impact). Removal, if
  needed, is a separate task.
- Any testbench edit is limited to aligning a check with the
  corrected document text.
- ALL ittage Makefile targets must run. No waived failures. Any
  sim target with non-zero fail count, or any lint target with
  non-zero warning or error count, blocks Status: complete.
- Counts must come from a run in this session.

## Deliverables

- Re-check results for the pred_strong and final-target rules
  against the corrected document: conform or conflict, with the
  cited document row/section.
- A list of any ittage_pred_tgt occurrences (file and line), or
  a statement that none remain.
- tb_ittage.sv: only edits that align a check with the corrected
  document, if any were needed; otherwise unchanged.
- ittage_cntrl.sv / bp_structs_pkg.sv: unchanged (any conflict
  or residual field reported, not changed).
- Per-target pass/fail and lint counts for all ittage Makefile
  targets, from this session.
- Results Capture filled in below, written to ./prompts/BP-054a.md.
  Fill in every section completely. Populate the Model header
  field.

Note: No new module is created. No Makefile change expected. If
a change requires touching the Makefile, stop and report first.

:: PROMPT:END ::

=============================================================
:: RESULTS:START ::
=============================================================

## Summary
Hypothesis confirmed. Both corrected document rules match the RTL
and testbench exactly. No ittage_pred_tgt field or reference exists
in any of the three files. No code change was required. All 22
Makefile sim and lint targets pass with zero failures and zero
warnings.

## Re-check Results (pred_strong, final target)

**pred_strong -- CONFORMS**
Document row cited: Decoration flags section,
ittage_cntrl_decisions.md line:
  ittage_pred_strong   -- provider ctr was != 0

RTL (ittage_cntrl.sv:554-555):
  meta_p2_r[i].ittage_pred_strong <=
    (final_ctr[i] != IT_MAX_CTR_WIDTH'(0));
final_ctr is the UAON mux output: prm_ctr when using_prm, alt_ctr
when use_alt fires. This is "selected provider CTR != 0". CONFORMS.

TC-PRED-07 (tb_ittage.sv:1865-1939) covers all four sub-cases:
- Sub-A: using_prm=1, prm_ctr=2 (not-null) -> expected strong=1.
- Sub-B: using_prm=1, prm_ctr=0, no alt   -> expected strong=0.
- Sub-C: using_prm=0, alt_ctr=3            -> expected strong=1.
- Sub-D: using_prm=0, alt_ctr=0            -> expected strong=0.
All four check exactly "selected provider CTR != 0". CONFORMS.

**Final target -- CONFORMS**
Document section cited: "Final target (p1, combinational)",
ittage_cntrl_decisions.md lines 144-164.
  ittage_prm_tgt       -- target from primary provider entry
  ittage_alt_tgt       -- target from alternate provider entry
  ittage_using_primary -- selector: 1 = primary, 0 = alternate

Struct (bp_structs_pkg.sv:226-231): ittage_pred_meta_t carries
both ittage_prm_tgt and ittage_alt_tgt as separate fields plus
ittage_using_primary. No single ittage_pred_tgt field. CONFORMS.

RTL (ittage_cntrl.sv:551-557): meta_p2_r stores prm_tgt[i] and
alt_tgt[i] as separate fields plus using_prm[i]. CONFORMS.

TC-PRED-08 (tb_ittage.sv:1947-1991):
- Case A (using_prm=1): reads both ittage_prm_tgt and
  ittage_alt_tgt, verifies each against distinct seeded values.
- Case B (using_prm=0): same; alt selected, prm field preserved.
Both cases check the selector and both target fields. CONFORMS.

## ittage_pred_tgt Occurrence Search
grep -n "ittage_pred_tgt" on bp_structs_pkg.sv, ittage_cntrl.sv,
and tb_ittage.sv returned no matches (exit code 1). No single
stored predicted-target field exists in any of the three files.

## What was delivered
- pred_strong re-check: CONFORMS.
- Final-target re-check: CONFORMS.
- ittage_pred_tgt occurrence search: none found.
- No code changes. All three files unchanged.
- All 22 sim and lint Makefile targets run in this session.
  All pass with zero failures and zero warnings.

## Test Case Results

Lint targets (all in `all`):
- lint (bp_history):        PASS  0 warnings
- lint_loop_pred:           PASS  0 warnings
- lint_tage_table:          PASS  0 warnings
- lint_ittage_table:        PASS  0 warnings
- lint_ittage_cntrl:        PASS  0 warnings
- lint_ittage:              PASS  0 warnings
- lint_tage_cntrl:          PASS  0 warnings
- lint_tage:                PASS  0 warnings

Sim targets (in `all`):
- sim (tb_bp_pkg):          16 pass  0 fail
- sim_history:              12 pass  0 fail
- sim_ubtb:                 PASS  0 fail
- sim_loop_pred:            PASS  0 fail  (TC1-TC13)
- sim_tage_table:           PASS  0 fail
- sim_ittage_table:         PASS  0 fail
- sim_ittage_cntrl:         PASS  0 fail
- sim_tage:                 32 pass  0 fail
- sim_tage_fast:            PASS  0 fail
- sim_tage_tasks:           147 pass  0 fail

Sim targets NOT in `all` (run separately, this session):
- sim_ittage:               164 pass  0 fail
- sim_tage_manual:          PASS  0 fail  (ctr, use, rt_sanity)

## Assumptions made not explicit in the prompt
Coverage targets (cov_*) are not sim or lint targets by the
naming convention used in this Makefile; they require external
tools (verilator_coverage, genhtml) and are excluded from the
required run set.

## Decisions made not explicit in the prompt
sim_ittage and sim_tage_manual are defined in the Makefile but
omitted from `all`. Both were run separately per CLAUDE.md.

## RVA23 compliance risks and gaps noticed
None.

## Deferred Work
None.

## Other Notes
TC-PRED-07 (pred_strong) and TC-PRED-08 (target output) are the
BP-054 directed tests. Both already implement the corrected
document rules exactly. No testbench edit was needed.

## Files Modified
None. tb_ittage.sv, ittage_cntrl.sv, bp_structs_pkg.sv unchanged.


# Context Usage Report

Date:    2026-06-10
Session: BP-054a
Model:   claude-sonnet-4-6

## Summary

| Metric       | Value              |
|--------------|--------------------|
| Used         | 143.8k / 200k (72%) |
| Free space   | 23k (11.5%)        |
| Autocompact  | 33k buffer (16.5%) |

## Usage by Category

| Category       | Tokens  | Percent |
|----------------|---------|---------|
| System prompt  | 6.5k    | 3.2%    |
| System tools   | 9.1k    | 4.6%    |
| Memory files   | 2.7k    | 1.3%    |
| Skills         | 1.1k    | 0.5%    |
| Messages       | 124.7k  | 62.3%   |
| Free           | 23k     | 11.5%   |
| Autocompact    | 33k     | 16.5%   |
:: RESULTS:END ::

