=============================================================
# Task Header
=============================================================
:: HEADER:START ::
| Field        | Value                   | Notes                    |
|--------------|-------------------------|--------------------------|
| Task ID      | BP-018                  |                          |
| Date         | 2026.04.09              |                          |
| Module       | ubtb.sv                 |                          |
| Run time     | 2m.26s                  |                          |
| Ctx %        | 24%                     |                          |
| Model        | Sonnet 4.6 medium       |                          |
| Resume sha   | 38390696-9eb5-42ee-9a48-3b18fc96c624 |             |
Task:   [ ] experiment  [ ] implementation  [ ] debug       [x] cleanup [ ] testbench
Status: [ ] in-progress [x] complete        [ ] abandoned
# Overview of task
Cleanup pass: CLI-012. Apply port naming convention
<signal>_<pipestage> to ubtb.sv. Three port renames:
  pred_pc -> pred_pc_p0
  pred    -> pred_p1
  upd     -> upd_u0
Update tb_ubtb.sv to match. No logic changes.
All 10 existing tests must pass.
:: HEADER:END ::
=============================================================
# Paste c.code console output and c.ai discussion
=============================================================
:: DISCUSSION:START ::
# Results Discussion
## Claude.code Console Output

 Summary:       

  - ubtb.sv: Three ports renamed — pred_pc→pred_pc_p0, pred→pred_p1, upd→upd_u0.
   All internal references (generate block, always_ff block, header comments)
  updated accordingly.                                                          
  - tb_ubtb.sv: Port connection names in both dut1 and dut2 instantiations
  updated to match. Local signal names unchanged.                               
  - Sim: Verilator 5.020 clean, zero warnings, all 10 tests PASS.

## My Assessment

Nothing significant, see CLAUDE.md Updates below

## Claude.ai Assessment

Clean pass. 10/10, zero warnings, minimal diff.
The double-rename artifact (pred_pc_p0_p0) was caught and corrected within the session — same pattern as the lp_lp_pred_is_loop issue in BP-017b. Worth noting for CLAUDE.md: sequential string replace on names that share substrings needs ordering care. You flagged a CLAUDE.md update for non-ASCII in console output — the double-rename artifact is a separate issue worth adding as well.

## Follow-on Actions
- [x] Update PROJECT_STATUS.md: close CLI-012.
## CLAUDE.md Updates
Add avoid non-ascii characters in console output
## Other Planning File Updates
None
:: DISCUSSION:END ::
=============================================================
# Claude.code Prompt
=============================================================
:: PROMPT:START ::
## Task ID
BP-018

## Context Loaded
@frontend/branch_predictor/rtl/ubtb.sv
@frontend/branch_predictor/tb/tb_ubtb.sv
@prompts/BP-018.md

## Hypothesis
ubtb.sv port names do not follow the project port naming
convention (<signal>_<pipestage>). Three ports need
renaming. No logic changes required.

## Background
CLI-012. Project port naming convention:
  <signal>_<pipestage>
  p0/p1/p2 for prediction path stages.
  u0/u1    for update path stages.

ubtb pipeline timing (from ubtb.sv header):
  s0: pred_pc presented. Index and tag derived comb.
  s1: mem is registered. pred output is comb from mem.
      pred valid at start of s1.
  Update: synchronous write on clk posedge when upd.valid.

Port renames required:
  pred_pc -> pred_pc_p0   (input presented at p0/s0)
  pred    -> pred_p1      (output valid at p1/s1)
  upd     -> upd_u0       (update input presented at u0)

## Binding Previous Decisions
- Port naming convention: <signal>_<pipestage>.
  p0/p1/p2 for prediction path. u0/u1 for update path.
  Established in bp_cluster.md and applied to all
  other BP cluster modules.
- ubtb_pred_t and ubtb_upd_t struct types do not change.
- No interface document exists for ubtb -- no doc
  updates required beyond the RTL and testbench.
- NUM_PRED_SLOTS parameter and all internal signal
  names are unchanged.

## Specific Requirements

1. In ubtb.sv rename the three module ports:
     pred_pc -> pred_pc_p0
     pred    -> pred_p1
     upd     -> upd_u0
   Update all internal references to these port names
   throughout the module body. Internal signal names
   that are not ports (slot_pc, slot_idx, uidx, etc.)
   are not renamed.

2. In tb_ubtb.sv update both DUT instantiations
   (dut1 and dut2) to use the new port names in the
   port connection list:
     .pred_pc  -> .pred_pc_p0
     .pred     -> .pred_p1
     .upd      -> .upd_u0
   The local signal names in the testbench (pred_pc,
   pred1, upd1, pred_pc2, pred2, upd2) are not renamed
   -- only the port connection names in the instantiation
   change.

3. Run the full sim. All 10 existing tests must PASS.
   Capture sim output in Results Capture.

## Constraints
- Modify ubtb.sv and tb_ubtb.sv only.
- Do not rename any internal signals that are not ports.
- Do not rename any local testbench signals.
- Do not modify any test logic.
- Do not modify bp_structs_pkg.sv or bp_defines_pkg.sv.
- All 10 existing tests must pass after rename.
- Verilator 5.020 clean: -Wall -Wno-DECLFILENAME
  -Wno-IMPORTSTAR -Wno-UNUSED --timing
- 80-column line limit. 2-space indent. ASCII comments.

## Deliverables
1. frontend/branch_predictor/rtl/ubtb.sv
   (three port renames and internal reference updates)
2. frontend/branch_predictor/tb/tb_ubtb.sv
   (port connection names in dut1 and dut2 updated)
3. Sim run showing all 10 tests PASS
4. Results Capture filled in below
:: PROMPT:END ::
=============================================================
# Results Capture
=============================================================
:: RESULTS:START ::
## Summary
Three port renames applied to ubtb.sv (pred_pc->pred_pc_p0,
pred->pred_p1, upd->upd_u0). All internal references updated.
tb_ubtb.sv dut1 and dut2 port connections updated to match.
Verilator 5.020 clean. All 10 existing tests PASS.

## Test Matrix (testbench sessions only, omit otherwise)
N/A -- port rename only, no new test logic.

## What was delivered
1. frontend/branch_predictor/rtl/ubtb.sv
   - Port pred_pc renamed to pred_pc_p0 (port declaration + 2
     internal references: comment lines and generate block assign)
   - Port pred renamed to pred_p1 (port declaration + 6 references
     in always_comb generate block)
   - Port upd renamed to upd_u0 (port declaration + 10 references
     in always_ff update block)
2. frontend/branch_predictor/tb/tb_ubtb.sv
   - dut1 port connections: .pred_pc->.pred_pc_p0,
     .pred->.pred_p1, .upd->.upd_u0
   - dut2 port connections: same three renames
   - Local signal names unchanged (pred_pc, pred1, upd1,
     pred_pc2, pred2, upd2)

## Test Case Results
TC1  -- Reset state:        PASS
TC2  -- Single write/hit:   PASS
TC3  -- Miss:               PASS
TC4  -- Tag collision:      PASS
TC5  -- Way replacement:    PASS
TC6  -- br_type coverage:   PASS
TC7  -- carry bit:          PASS
TC8  -- Overwrite:          PASS
TC9  -- Dual prediction:    PASS
TC10 -- Read-during-write:  PASS
ALL TESTS PASSED

Sim command: make sim_ubtb
Verilator flags: --sv -Wall -Wno-UNUSED -Wno-IMPORTSTAR --timing
  -Wno-DECLFILENAME -Wno-VARHIDDEN
Exit status: 0 warnings, 0 errors

## Assumptions made not explicit in the prompt
- Header comment lines referencing pred_pc (lines 9 and 11)
  were also updated to pred_pc_p0 since they describe the port
  by name. The prompt said not to rename internal signals, but
  these are comments describing the port itself, not signal
  declarations. Updated for consistency.

## Decisions made not explicit in the prompt
- Renamed replace_all for pred_pc first (caught port + body),
  then fixed accidental double-rename artifact (pred_pc_p0_p0
  -> pred_pc_p0) that resulted from the sequential edit order.
  Final file is correct.

## RVA23 compliance risks and gaps noticed
None. This is a pure naming cleanup with no logic changes.

## Deferred Work
- Close CLI-012 in PROJECT_STATUS.md (noted in Discussion).

## Other Notes
None.
:: RESULTS:END ::

