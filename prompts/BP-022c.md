<!-- SPDX-License-Identifier: Apache-2.0                        -->
<!-- Copyright (c) 2026 Jeff Nye, uarchlabs.com                 -->
<!-- SPDX-FileCopyrightText: 2026 Jeff Nye <jeff@uarchlabs.com> -->
=============================================================
# Task Header
=============================================================
:: HEADER:START ::
| Field        | Value                   | Notes                    |
|--------------|-------------------------|--------------------------|
| Task ID      | BP-022c                 |                          |
| Date         | 2026.04.10              |                          |
| Module       | bp_defines_pkg.sv       |                          |
| Run time     | 1m.46s                  |                          |
| Ctx %        | 19%                     |                          |
| Model        | Sonnet 4.6 medium       |                          |
| Resume sha   | e9ec3ae9-4f05-4b24-96d9-dcc25ddd9796 |             |
| PA session  | 028 | |
Task:   [ ] experiment  [ ] implementation  [ ] debug  [x] cleanup  [ ] testbench
Mode:   [x] automated   [ ] manual
Status: [ ] in-progress [x] complete        [ ] abandoned
# Task Overview
  Remove SC_TBL_INDEX_BITS and commented alias block from bp_defines_pkg.sv.
  Lint-only verification. No sim changes expected.
:: HEADER:END ::
=============================================================
# Paste c.code console output and c.ai discussion
=============================================================
:: DISCUSSION:START ::
# Results Discussion
## Claude.code Console Output

  Consumer search: Grepped all files under frontend/branch_predictor/ for       
  SC_TBL_INDEX_BITS. Only hit was its own definition — no active consumers.
                                                                                
  Deletions made to bp_defines_pkg.sv:                                          
  1. Lines 165-170 (original): the commented-out alias block (MAX_IDX_WIDTH,    
  MAX_TAG_WIDTH, MAX_EPC_WIDTH, MAX_USE_WIDTH, MAX_CTR_WIDTH, MAX_VAL_WIDTH) —  
  dead code from BP-022a/b.                                                   
  2. Lines 180-181 (original): the SC_TBL_INDEX_BITS comment + localparam — no  
  consumers, does not follow SC_MAX_* naming convention. SC_MAX_IDX_WIDTH (= 10)
   remains as the canonical name.                                               
                                 
  Lint: make lint exits 0, zero warnings. No other files modified. 

## My Assessment
Nothing required
## Claude.ai Assessment
Nothing required
## Follow-on Actions
Nothing required
## CLAUDE.md Updates
Nothing required
## Other Planning File Updates
Nothing required
:: DISCUSSION:END ::
=============================================================
# Claude.code Prompt
=============================================================
:: PROMPT:START ::
## Task ID
BP-022c

## Context Loaded
@frontend/branch_predictor/rtl/bp_defines_pkg.sv

## Hypothesis
Two dead-code items remain in bp_defines_pkg.sv from BP-022b.
Removing them will leave the file clean with no functional change.

## Background
BP-022b removed SC_NUM_MAIN_TBLS and eight backward-compat aliases
from bp_defines_pkg.sv. Two items were deferred:

1. SC_TBL_INDEX_BITS: a localparam with no confirmed active consumers.
2. A commented-out alias block near lines 165-170: dead code left from
   the alias removal pass in BP-022a/BP-022b.

## Binding Previous Decisions
- bp_defines_pkg.sv contains parameters only. No structs, no logic.
- Import order is mandatory: bp_defines_pkg first, bp_structs_pkg second.
- All MAX_* field-width parameters now carry predictor prefix
  (TAGE_MAX_*, SC_MAX_*, IT_MAX_*). SC_TBL_INDEX_BITS does not follow
  this convention and has no active consumers.

## Specific Requirements
1. Search all files under frontend/branch_predictor/ for any reference
   to SC_TBL_INDEX_BITS before removing it. If any active consumer is
   found, stop and report. Do not remove it speculatively.
2. Remove SC_TBL_INDEX_BITS localparam from bp_defines_pkg.sv if no
   active consumers are found.
3. Remove the commented-out alias block (near lines 165-170) from
   bp_defines_pkg.sv. This is dead code only -- do not uncomment or
   restore any alias.
4. No other changes to any file.

## Constraints
- Modify bp_defines_pkg.sv only. No other file may be touched.
- No functional changes. Deletions only.
- If SC_TBL_INDEX_BITS has any active consumer, stop and report
  before making any change.
- Line width 80 columns maximum.

## Deliverables
1. Updated frontend/branch_predictor/rtl/bp_defines_pkg.sv.
2. Lint result for bp_defines_pkg consumers. Use the existing lint
   target (make lint_bp_pkg or equivalent). Must exit zero with zero
   warnings under existing VER_FLAGS suppressions.
3. Results Capture filled in below.
:: PROMPT:END ::
=============================================================
# Results Capture
=============================================================
:: RESULTS:START ::
## Summary
Both dead-code items removed from bp_defines_pkg.sv. No functional
change. Lint exits zero with zero warnings.

## Test Matrix
N/A -- cleanup task, no testbench session.

## What was delivered
1. frontend/branch_predictor/rtl/bp_defines_pkg.sv -- two deletions:
   - SC_TBL_INDEX_BITS localparam and its comment line (was ~line 181).
   - Commented-out alias block (was lines 165-170: MAX_IDX_WIDTH,
     MAX_TAG_WIDTH, MAX_EPC_WIDTH, MAX_USE_WIDTH, MAX_CTR_WIDTH,
     MAX_VAL_WIDTH).
2. Lint result: make lint -> exit 0, zero warnings.

## Test Case Results
N/A

## Assumptions made not explicit in the prompt
- "make lint_bp_pkg" does not exist. The equivalent target is "make
  lint", which invokes verilator --lint-only on bp_defines_pkg.sv,
  bp_structs_pkg.sv, and bp_history.sv under project VER_FLAGS.
  This target was used and passed.

## Decisions made not explicit in the prompt
- Consumer search scope was frontend/branch_predictor/ (all files,
  all subdirectories). SC_TBL_INDEX_BITS appeared only in its own
  definition. No active consumer found; removal proceeded.
- The trailing blank line between SC_NUM_ALL_TBLS and SC_TBL_ENTRIES
  was preserved; it was not part of the removed block.
- The double blank line at lines 163-164 (after TAGE_MAX_FH2) was
  left as-is; it is a minor formatting artifact, not a defect.

## RVA23 compliance risks and gaps noticed
None introduced. Removed items were dead code only.

## Deferred Work
None. BP-022 cleanup series complete.

## Other Notes
SC_TBL_INDEX_BITS = SC_MAX_IDX_WIDTH (= 10). SC_MAX_IDX_WIDTH
remains in the file and is the canonical name per the prefixed
naming convention (SC_MAX_*). No data loss.

## Files Modified
Not captured

:: RESULTS:END ::
